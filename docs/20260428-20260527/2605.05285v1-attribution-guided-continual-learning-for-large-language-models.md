---
title: Attribution-Guided Continual Learning for Large Language Models
title_zh: 基于归因引导的大语言模型持续学习
authors: "Yazheng Liu, Yuxuan Wan, Rui Xu, Xi Zhang, Sihong Xie, Hui Xiong"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.05285v1"
tags: ["query:continual"]
score: 10.0
evidence: 通过基于参数重要性的梯度调节缓解大语言模型中的灾难性遗忘
tldr: 大语言模型在持续学习新任务时会遗忘旧知识，现有缓解方法缺乏对内部知识分布的语义感知，难以区分应保留或更新的参数。提出归因引导的持续微调框架，通过估计每层Transformer中逐元素的参数重要性来调制梯度，让旧任务关键参数更新幅度更小。实验在持续学习基准上一致优于基线，显著提升旧任务保留且新任务性能有竞争力。该工作为缓解LLM灾难性遗忘提供了参数重要性的语义量化方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.05285v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1368, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.05285v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1396, \"height\": 609, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.05285v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1426, \"height\": 628, \"label\": \"Figure\"}]"
motivation: 现有数据重放、参数冻结或正则化方法缺乏语义感知，无法有效区分哪些参数对旧任务重要而应保留、哪些可塑性更新。
method: 提出归因引导的持续微调，估计Transformer每层中任务特定的逐元素参数重要性，并用其得分调制梯度，减少对旧任务关键参数的更新。
result: 在多个持续学习基准上，本方法一致超越基线，更好地保留旧任务，同时在新任务上维持有竞争力的表现。
conclusion: 通过归因量化参数重要性并指导梯度更新，可有效缓解LLM的灾难性遗忘，为持续学习提供新的语义感知方案。
---

## 摘要
大语言模型在持续学习中常面临灾难性遗忘：在依次学习新任务后，对先前任务的性能下降。现有方法通过数据重放、参数冻结或正则化来缓解遗忘，但这些方法缺乏对LLM内部知识分布的语义感知，因而无法区分应保留或更新的参数。我们提出了一种基于归因引导的LLM持续微调框架。该方法估计每个Transformer层中任务特定的、逐元素的参数重要性，并利用这些分数调节梯度。对先前任务重要的参数获得较小的更新，而相关性低的参数则保持可塑性以学习新任务。在持续学习基准上的实验表明，我们的方法一致优于基线方法，在保持新任务有竞争力的性能的同时，更好地保留了对旧任务的知识。

## Abstract
Large language models (LLMs) often suffer from catastrophic forgetting in continual learning: after learning new tasks sequentially, they perform worse on earlier tasks. Existing methods mitigate catastrophic forgetting by data replay, parameter freezing, or regularization. However, these methods lack semantic awareness of internal knowledge distribution in LLMs. As a result, they cannot distinguish parameters that should be preserved or updated. We propose an attribution-guided continual fine-tuning framework for LLMs. Our method estimates task-specific, element-wise parameter importance in each Transformer layer and uses these scores to modulate gradients. Parameters important to previous tasks receive smaller updates, while less relevant ones remain plastic for learning new tasks. Experiments on continual learning benchmarks show that our method consistently outperforms baselines, achieving better retention of old tasks while maintaining competitive performance on new tasks.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义
- **核心问题**：大语言模型 (LLM) 在顺序学习多个任务时会发生灾难性遗忘——新任务的微调会大幅损害模型在旧任务上的表现。
- **现有方法局限**：数据重放 (replay)、参数冻结 (freezing)、正则化 (regularization) 等经典持续学习手段缺少对 LLM 内部知识分布的**语义感知**，无法从机制层面区分哪些参数对旧知识至关重要因而必须保护、哪些参数可以留给新任务更新。
- **论文动机**：作者通过实验发现，在单任务独立微调下，不同任务依赖的 Top–K 重要参数重叠很低；但在持续学习中，两种任务的重要参数相似度显著上升。这意味着新任务的学习会**覆盖**旧任务原本所依赖的参数，导致干扰和遗忘。因此，论文提出一种基于**参数归因重要性**来引导梯度更新的持续微调框架，从“哪些参数不想被改写”的角度来缓解遗忘。

## 2. 论文提出的方法论
### 2.1 核心思想
- 为每个任务**逐元素地估计** Transformer 每一层中每个参数的重要性（归因分数）。
- 在后续学习新任务时，依据历史任务的重要性得分，**自适应地缩放梯度**：对旧任务至关重要的参数只允许很小的更新，而与之无关的参数可以自由更新，以此减少新旧任务间的参数干扰。

### 2.2 关键技术细节
#### 2.2.1 基于 LRP 的参数级归因
- **基础工具**：使用 Layer-wise Relevance Propagation (LRP) 并将其扩展到 Transformer 的**参数归因**上（传统 LRP 通常只归因到输入特征）。
- **核心推导**：
  - 利用 AttnLRP 中双线性运算的归因规则，对 FFN 层和 Multi-Head Attention (MHA) 层分别推导出参数级的相关性传播公式（定理 4.1、命题 4.2、命题 4.3）。
  - 例如，对线性变换 `C = PW + B`，给出输入 `P`、权重 `W`、偏置 `B` 各自应分配的相关性，且保持总和守恒。
  - 对整个 Transformer block，从输出 logits 开始，反向传播相关性，最终得到每个参数（包含注意力中的 Q/K/V/O 投影权重、前馈网络权重、偏置等）对于预测下一个 token 的贡献分数 `R(W)`。
- **LoRA 适配**：对于 LoRA 微调，同样可以归因到可训练的 A/B 矩阵以及冻结的骨干参数上。

#### 2.2.2 任务重要性先验估计
- 对每个任务 `T_t`，先单独微调一个模型，挑选出能够**正确生成**的样本 `C_t`。
- 对每个正确样本计算参数归因图，经过 sign–log 归一化后，对每个参数保留其跨样本的** Top-K 归一化相关性**并取均值，得到该任务最终的逐元素重要性分数 `R(W^{(l,t)})`。

#### 2.2.3 归因引导的持续学习
- 当学习第 `t` 个任务时，对所有历史任务 `τ < t` 的 Importance map 取**元素级最大值**作为历史重要性 `R̄(W^{(l,t)})`。
- 在微调的梯度更新中，将其作为门控因子：
  ```
  调制梯度 = (1 - R̄(W)) ⊙ 原始梯度
  ```
- 历史重要性接近 1 的参数更新幅度趋近于 0（被保护），接近 0 的参数则基本不受限制，保持可塑性。
- 该方法可同时应用于全参数微调与 LoRA 微调。

## 3. 实验设计
- **数据集与基准**：论文声称在多个持续学习基准（continual learning benchmarks）上进行了评估，并设置了**全量监督微调**和**基于 LoRA 的适配**两种实验场景。正文中提到了一个先导性分析实验，使用了**摘要生成**和**代码补全**这两个任务。
- **对比方法**：声称与现有持续学习基线进行了比较，但**未在提供的提取文本中列出具体方法名称或数据集细节**（如序列任务数量、评测指标等）。提取内容仅包含引言、相关工作及方法论，缺少“实验”章节的详细描述。
- **备注**：当前提供的论文片段中没有呈现具体的实验设置表格、数据集统计、基线名称及性能数值，无法进一步展开。

## 4. 资源与算力
- 论文提取文本中**未提及**所使用的 GPU 型号、数量、训练总时长或显存消耗等算力信息。
- 由于缺失实验章节，无法给出任何关于资源开销的说明。

## 5. 实验数量与充分性
- **实验组数**：无法从现有文本中确定。文中仅宣称“在持续学习基准上的实验表明我们的方法始终优于基线”，没有列出具体的实验数目、消融实验或显著性检验。
- **充分性与公平性评价**：在无详细实验数据的情况下，难以判断实验是否充分、对比是否公平。考虑到论文本身可能将完整实验放置在未提取的部分，因此此处无法做出客观评判。从方法设计的动机和理论推导看，框架具有合理性，但其实证有效性需要依赖未展示的实验结果。

## 6. 论文的主要结论与发现
- 不同任务在单任务微调下依赖的参数子集高度分离，而持续学习会导致它们的参数重叠加剧，这是遗忘的一个重要诱因。
- 通过 LRP 为每个任务计算出逐元素参数重要性，并据此在后续任务中**梯度级保护**旧知识，可以显著**减轻灾难性遗忘**。
- 所提归因引导框架在保留旧任务表现的同时，能够在新任务上保持**有竞争力的性能**，优于传统的不区分参数重要性的方法。

## 7. 优点
- **语义感知的遗忘缓解**：首次从 LLM 内部知识分布（参数重要性）出发解决持续学习中的遗忘，而非盲目重放或整体冻结。
- **精细的梯度调制**：采用元素级的重要性分数而非层级或模块级的粗粒度控制，可以在保护旧知识的同时最大化模型的可塑性。
- **完整的理论推导**：将 LRP 严格扩展至 Transformer 的 FFN 和 MHA 的参数归因，给出了详细、守恒的传播公式（含 LoRA 变体），为后续研究提供了可复用的工具。
- **模型无关性**：框架可同时用于全参数微调和 LoRA 微调，通用性较好。

## 8. 不足与局限
- **计算开销**：需对每个旧任务的正确样本进行逐样本的参数归因，且归因过程涉及逐层的反向传播，可能带来显著的额外训练前计算开销（特别是任务数增多时）。
- **实验信息缺失**：提供的论文文本未包含任何实质性的实验对比（无数据集、无具体基线方法、无性能数据、无消融实验），无法验证其宣称的优越性，也无法评估方法的鲁棒性和可复现性。
- **存储负担**：每个已学任务需要存储一份完整的逐元素重要性 map，任务数量增长时存储压力线性增大，且需要设计合理的累积策略（文中仅用 max，可能不是最优）。
- **泛化性风险**：重要性估计依赖于从任务特定微调模型上选取的“正确样本”，当初始任务数据很少或模型在训练早期无法正确生成时，重要性估计可能不准确。
- **未明确讨论的问题**：缺乏对超参数（如 Top-K 中的 K、归因的归一化方式）的敏感性分析，也未提及与 Elastic Weight Consolidation (EWC) 等经典参数重要性方法的理论或实验对比（尽管 EWC 使用的是 Fisher 信息矩阵），说服力受限。

（完）
