---
title: Noise-Aware Visual Representation Learning for Medical Visual Question Answering
title_zh: 面向医学视觉问答的噪声感知视觉表征学习
authors: "I Putu Adi Pratama, Bahadorreza Ofoghi, Atul Sajjanhar, Shang Gao"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.05535v1"
tags: ["query:med-lifelong"]
score: 9.0
evidence: 噪声感知视觉表征学习用于医学视觉问答，解读医学图像
tldr: 医学视觉问答有潜力辅助临床决策，但现有方法忽略视觉表征中的噪声。本文提出噪声感知框架，在视觉嵌入映射至大语言模型前引入去噪自编码器，预训练重构干净嵌入。实验表明，该框架提升了医学视觉问答的准确性和抗噪能力，使模型更可靠地解读医学图像。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05535v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1199, \"height\": 837, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05535v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1244, \"height\": 474, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05535v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 699, \"height\": 193, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05535v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1257, \"height\": 436, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05535v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1089, \"height\": 307, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05535v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1096, \"height\": 309, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05535v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1086, \"height\": 309, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05535v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1098, \"height\": 311, \"label\": \"Table\"}]"
motivation: 噪声感知视觉表征学习用于医学视觉问答，解读医学图像。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
医学视觉问答（Med-VQA）通过使AI模型能够解读医学图像并回答与临床相关的问题，在临床决策支持方面展现出巨大潜力。近期方法通常通过轻量级映射网络将现成的视觉编码器与大型语言模型（LLM）连接起来，以降低计算成本。然而，这些方法常常忽视了处理视觉表征中的噪声和微小无关变化的重要性。为了解决这些挑战，我们提出了一种噪声感知的Med-VQA框架，该框架在视觉嵌入映射到LLM的输入空间之前，引入了一个去噪自编码器。该去噪自编码器经过预训练，能够从损坏的输入中重建出干净的视觉嵌入，促使模型学习对噪声不太敏感的鲁棒视觉表征。随后，使用多层感知机（MLP）将得到的嵌入投影到语言模型的嵌入空间中，形成视觉前缀标记，为LLM提供图像信息。为了实现高效适应而无需完全重新训练，我们采用了参数高效微调方法，即低秩适应（LoRA）。在SLAKE和PathVQA基准上对所提出的方法进行了评估。实验结果表明，该方法对噪声输入嵌入的鲁棒性有所提高，同时在多个评价标准上保持了有竞争力的干净性能。这些发现表明，学习更鲁棒的视觉表征能够提升Med-VQA的性能和鲁棒性。

## Abstract
Medical visual question answering (Med-VQA) has strong potential for clinical decision support by enabling AI models to interpret medical images and answer clinically relevant queries. Recent approaches typically connect off-the-shelf vision encoders with large language models (LLMs) through lightweight mapping networks to reduce computational cost. However, these methods often overlook the importance of handling noise and small irrelevant changes in visual representations. To address these challenges, we propose a noise-aware Med-VQA framework that incorporates a denoising autoencoder before visual embeddings are mapped into the input space of an LLM. The denoising autoencoder is pretrained to reconstruct clean visual embeddings from corrupted inputs, encouraging the model to learn robust visual representations that are less sensitive to noise. The resulting embeddings are then projected into the language model embedding space using a multi-layer perceptron (MLP), forming visual prefix tokens that provide image information to the LLM. To enable efficient adaptation without full retraining, we employ parameter-efficient fine-tuning using low-rank adaptation (LoRA). The proposed method is evaluated on the SLAKE and PathVQA benchmarks. Experimental results show improved robustness to noisy input embeddings while maintaining competitive clean performance across multiple evaluation criteria. These findings suggest that learning more robust visual representations can enhance Med-VQA performance and robustness.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：现有医学视觉问答（Med-VQA）系统通常使用现成的视觉编码器（如CLIP）和大型语言模型（LLM），通过轻量级映射网络（如MLP）连接。然而，这些方法普遍忽略了视觉嵌入中存在的噪声和微小无关扰动，这些扰动会通过映射传播给LLM，影响下游回答的稳定性与准确性。
- **研究动机**：医学图像在采集、传输过程中易引入各种噪声，导致视觉嵌入质量下降。因此，需要一种能提升视觉表征鲁棒性的方法，以减少噪声对最终问答的影响。去噪自编码器（Denoising Autoencoder, DAE）因其学习稳定、去噪表征的能力而成为合适的解决方案。
- **整体含义**：提出一种噪声感知的Med-VQA框架，在将视觉嵌入输入LLM之前，利用去噪自编码器对嵌入进行去噪，从而学习对噪声不敏感的鲁棒表征，提高问答模型的性能与鲁棒性。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：采用两阶段训练范式，将去噪表征学习与生成式对齐解耦。
  - **第一阶段**：训练一个去噪自编码器，从被高斯噪声损坏的视觉嵌入中重构原始干净嵌入，学习鲁棒表征。
  - **第二阶段**：冻结去噪自编码器的编码器，提取鲁棒潜在表征，并通过3层MLP映射为视觉前缀令牌，与文本嵌入拼接后输入LLM生成答案，并可选地使用LoRA高效微调LLM。
- **模型架构**：
  - **视觉编码器**：冻结的CLIP ViT-B/32，输出512维全局视觉嵌入。
  - **去噪自编码器**：瓶颈MLP结构，编码器将512维输入经256维隐藏层压缩至128维潜在空间，解码器对称重建。使用ReLU激活和0.1 dropout。
  - **视觉前缀映射**：3层MLP将128维潜在表征映射为8个前缀令牌，每个令牌维度为1600（GPT-2 XL的嵌入维度）。
  - **语言模型**：GPT-2 XL，采用因果语言建模生成答案。
- **训练流程与关键公式**：
  - **阶段1（去噪自编码器预训练）**：
    - 输入：纯净视觉嵌入 \( v \)，加入高斯噪声 \( \epsilon \sim \mathcal{N}(0, \sigma^2 I) \) 得到 \( \tilde{v} \)。
    - 编码：\( z = f_{\text{enc}}(\tilde{v}) \)；解码：\( \hat{v} = f_{\text{dec}}(z) \)。
    - 损失函数：Smooth L1损失
      \[
      \mathcal{L}_{\text{DAE}} = \frac{1}{N} \sum_{i=1}^N \begin{cases}
      \frac{1}{2}(\hat{v}_i - v_i)^2 & \text{if } |\hat{v}_i - v_i| < 1 \\
      |\hat{v}_i - v_i| - \frac{1}{2} & \text{otherwise}
      \end{cases}
      \]
  - **阶段2（Med-VQA训练）**：
    - 利用编码器得到潜在表征 \( z = f_{\text{enc}}(v) \)，映射为视觉前缀 \( P_v \)。
    - 输入序列构造：`[question: 问题词嵌入, context: P_v, answer 答案词嵌入]`。
    - 损失函数：标准语言建模交叉熵损失
      \[
      \mathcal{L}_{\text{VQA}} = -\sum_{t \in A} \log p_{\theta}(y_t | y_{<t}, q, I)
      \]
    - 微调方式：仅训练视觉映射器，或同时使用LoRA微调LLM的注意力参数（低秩矩阵）。
- **噪声注入策略**：预训练阶段噪声标准差 \( \sigma \in \{0.1, 0.5, 1.0\} \)；推理时也在嵌入中加入高斯噪声以评估鲁棒性。

### 3. 实验设计：使用了哪些数据集/场景，它的benchmark是什么，对比了哪些方法
- **数据集**：
  - SLAKE（642张图像，14,028个问答对）：放射科图像，包含开放和是/否问题。
  - PathVQA（4,998张病理图像，32,799个问答对）：组织病理学图像。
  - 均使用官方训练/验证/测试划分。
- **Benchmark与评估指标**：BLEU-1 (BL1)、BERTScore (BS)、F1值、准确率（总体及按开放、是/否分类）。
- **对比方法**：
  - **Baseline**：直接使用MLP将CLIP视觉嵌入映射到LLM输入空间。
  - **Ours w/ AE**：加入标准自编码器（无噪声注入）的变体。
  - **Ours w/ DAE**：本文所提去噪自编码器框架。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU型号、数量、训练时长等）。若未明确说明，也请指出这一点。
- **未说明**：论文没有提及所使用的GPU型号、数量、具体训练时长、显存占用等算力相关信息。仅描述了模型架构（ViT-B/32, GPT-2 XL）等模型规模，但未提供训练资源细节。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平
- **实验组次**：在SLAKE和PathVQA两个数据集上，分别进行了：
  - 干净评估性能对比（表2，含冻结和LoRA两种LLM设置，共6组对比）。
  - 鲁棒性评估：在3个等级的预训练噪声（σ=0.1, 0.5, 1.0）下，每个设置测试4个推理噪声水平（0, 0.1, 0.5, 1.0），计算Noisy Avg. Acc.（表3、5以及表4、6的横向对比）。
  - 定性示例展示（图2）。
- **公平性**：对比方法使用相同的视觉编码器和LLM，仅改变视觉嵌入的处理方式（直接映射、AE、DAE），控制变量较为严格。
- **充分性**：实验覆盖了两种数据集、两种微调策略、多种噪声层级，能够较全面地验证方法的有效性、鲁棒性和不同预训练噪声强度的影响。但缺乏与更先进适配器（如Q-Former、交叉注意力）的对比，消融实验也主要局限于有无噪声和不同噪声等级，未对DAE结构、前缀长度等进行消融。

### 6. 论文的主要结论与发现
- 引入去噪自编码器可得到更鲁棒的视觉表征，使得在视觉嵌入受到噪声干扰时，Med-VQA性能下降更小。
- 在SLAKE上，DAE方法（σ=0.5）在LoRA下Noisy Avg. Acc.从0.642提升至0.735，冻结设置下从0.473提升至0.713；在PathVQA上也观察到一致提升（LoRA下从0.514至0.568，冻结下从0.348至0.518）。
- 标准自编码器（AE）在干净条件下有时能获得稍高准确度，但其鲁棒性显著低于DAE，尤其在强噪声下。
- 中等强度的预训练噪声（σ=0.5）在大多数场景下实现了干净性能与鲁棒性的最佳平衡。
- 结果表明，嵌入级别的去噪是一种提升小规模医学数据集上Med-VQA系统鲁棒性的有效策略。

### 7. 优点：方法或实验设计上有哪些亮点
- **新颖的两阶段设计**：将去噪表征学习和生成式对齐解耦，简单易行，可直接插入现有冻结管线。
- **针对性解决噪声问题**：首次在Med-VQA中系统研究视觉嵌入的噪声鲁棒性，并用DAE在输入LLM前预处理。
- **系统的鲁棒性评估协议**：通过预训练阶段和推理阶段分别注入不同强度高斯噪声，量化评估模型在干净和噪声条件下的表现。
- **有效的实验结果**：在保持有竞争力干净性能的同时，显著提升对嵌入噪声的鲁棒性，特别是在大噪声下优势明显。
- **参数高效微调**：结合LoRA，在适应医学领域的同时避免灾难性遗忘。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **噪声假设单一**：仅使用高斯加性噪声模拟嵌入扰动，可能与实际医学图像中复杂的伪影、信号相关噪声等分布不符。
- **视觉映射方式简单**：采用浅层MLP映射，未与更先进的适配器（如Transformer查询、交叉注意力）进行对比，可能限制跨模态对齐的潜力。
- **实验范围较窄**：仅基于CLIP ViT-B/32和GPT-2 XL，未探索不同视觉编码器或更大语言模型的效果；消融实验不够丰富，未分析DAE结构、潜在维度、前缀长度等关键超参数的影响。
- **干净性能提升有限**：在某些设置下（如PathVQA），AE的干净性能略优于DAE，表明去噪训练可能在无噪声时带来轻微的表示能力损失。
- **推理噪声人为设定**：推理时仍使用高斯噪声注入进行评估，未在真实采集噪声图像上进行测试，与临床部署的关联性有限。
- **资源描述缺失**：未给出训练时间、GPU使用等算力细节，影响可复现性评估。

（完）
