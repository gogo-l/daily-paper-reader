---
title: "SpectraFlow: Unifying Structural Pretraining and Frequency Adaptation for Medical Image Segmentation"
title_zh: SpectraFlow：统一结构预训练与频率自适应用于医学图像分割
authors: "Zhiquan Chen, Haitao Wang, Guowei Zou, Hejun Wu"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14566v1"
tags: ["query:med-lifelong"]
score: 9.0
evidence: 低数据条件下医学分割的两阶段框架：结构感知预训练和边界导向解码
tldr: SpectraFlow针对标注稀缺问题，先用混合域平均流预训练结构感知编码器，再用边界导向解码。该方法减少纹理偏差，保持拓扑一致性和细节，显著提高低数据条件下医学图像分割质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.14566v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1217, \"height\": 997, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.14566v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1256, \"height\": 1207, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.14566v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 537, \"height\": 219, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.14566v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 561, \"height\": 457, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.14566v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 835, \"height\": 511, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.14566v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1203, \"height\": 1147, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.14566v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 451, \"height\": 314, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.14566v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1249, \"height\": 916, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.14566v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1268, \"height\": 580, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.14566v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1213, \"height\": 270, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.14566v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1256, \"height\": 322, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.14566v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1259, \"height\": 285, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.14566v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 996, \"height\": 204, \"label\": \"Table\"}]"
motivation: 低数据条件下医学分割的两阶段框架：结构感知预训练和边界导向解码。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
医学图像分割在低数据场景下仍具挑战性，稀缺的标注常常导致泛化能力差、边界模糊以及精细结构丢失。近期自监督预训练提高了可迁移性，但常表现出纹理偏差。相比之下，精确分割本质上是几何感知的，并依赖于拓扑一致性和精确的边界保持。为了解决这一问题，我们提出一个两阶段框架，将结构感知的编码器预训练与边界导向的解码相结合。在第一阶段，我们旨在学习结构感知的表示，用于低数据场景下的下游分割。为此，我们提出混合域均值流预训练（Mixed-Domain MeanFlow Pretraining），通过潜在传输回归在共享潜在空间中对齐图像和二值掩模，其中掩模作为条件结构引导而非预测目标，使得预训练任务无关。为了进一步提高稀缺监督下的训练稳定性，我们引入轻量级分散损失以防止表示坍缩。在第二阶段，我们用轻量级解码器微调预训练编码器，该解码器结合了直接注意力融合用于自适应跨尺度门控，以及频率方向动态卷积用于外观变化下的高频边界精细化。在 ISIC-2016、Kvasir-SEG 和 GlaS 数据集上的实验表明，与最先进方法相比持续性能提升，在低数据场景下鲁棒性提高，边界刻画更清晰。

## Abstract
Medical image segmentation remains challenging in low-data regimes, where scarce annotations often yield poor generalization and ambiguous boundaries with missing fine structures. Recent self-supervised pretraining has improved transferability, but it often exhibits a texture bias. In contrast, accurate segmentation is inherently geometry-aware and depends on both topological consistency and precise boundary preservation. To address this problem, we propose a two-stage framework that couples structure-aware encoder pretraining with boundary-oriented decoding. In Stage-1, we aim to learn structure-aware representations for downstream segmentation in low-data regimes. To this end, we propose Mixed-Domain MeanFlow Pretraining, which aligns images and binary masks in a shared latent space through latent transport regression, where masks act as conditional structural guidance rather than prediction targets, making the pretraining task-agnostic. To further improve training stability under scarce supervision, we incorporate a lightweight Dispersive Loss to prevent representation collapse. In Stage-2, we fine-tune the pretrained encoder with a lightweight decoder that combines Direct Attentional Fusion for adaptive cross-scale gating and Frequency-Directional Dynamic Convolution for high-frequency boundary refinement under appearance variation. Experiments on ISIC-2016, Kvasir-SEG, and GlaS demonstrate consistent gains over state-of-the-art methods, with improved robustness in low-data settings and sharper boundary delineation.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义
- **研究背景**：医学图像分割是计算机辅助诊断的关键，但在标注数据稀少时，模型容易出现泛化能力差、边界模糊、细小解剖结构丢失等问题。
- **核心动机**：现有自监督预训练（如掩码重建）通常偏向纹理复原，学习到的特征对结构几何不敏感；而准确的分割本质上需要几何一致性、拓扑连续和精确边界保持。因此，纹理偏差成为低数据条件下分割性能受限的重要原因。
- **整体含义**：该论文提出一个名为 **SpectraFlow** 的两阶段框架，旨在将学习重心从纹理转移到几何结构，缓解高频边界误差，从而在低标注场景下提升分割的稳定性和边界质量。

## 2. 方法论
### 第一阶段：结构感知编码器预训练——混合域均值流预训练
- **核心思想**：将训练图像与二值掩模（仅来自训练集，但不作为预测目标）映射到共享的潜在空间，通过**潜在传输回归**学习几何一致的表示，使特征随着结构而非纹理变化。
- **关键技术细节**：
  - 使用均值流（MeanFlow）框架：对编码器输出的潜在特征 `z0` 施加扰动 `zt = α_t z0 + σ_t ε`，然后学习长时间跨度的传输方向 `h_θ(zt, t, s)`，通过自蒸馏回归目标优化编码器，强化潜在空间的平滑性和一致性。
  - 掩模仅作为条件结构输入，不参与分割损失，保持预训练任务无关性，体现“结构引导”而非“语义监督”。
  - 引入轻量级**分散损失 (Dispersive Loss)**：将批次内所有表示视为负对，施加排斥力，增大样本间距离，从而防止表示坍缩，提升训练稳定性和特征多样性。其公式为：
    ```
    L_Disp = log( 1/(B(B-1)) Σ_{i≠j} exp(-||h_i - h_j||²/τ) + ε )
    ```
  - 混合域训练：同时输入原始图像和二值掩模（掩模通道复制以匹配输入维度），以固定采样率混合成批次。

### 第二阶段：分割微调——边界导向解码
- **整体流程**：载入第一阶段预训练编码器（仅微调解码器及编码器最后一个模块），附加轻量解码器进行有监督学习。
- **关键模块**：
  - **直接注意力融合 (DAF)**：对中层跳跃连接的特征进行自适应门控融合，利用局部和全局上下文生成门控掩模 `M`，通过 `f_fused = 2·f_dec ⊙ M + 2·f_skip ⊙ (1-M)` 抑制背景噪声、强化边界相关细节。
  - **频率方向动态卷积 (FDConv)**：在解码器的细化模块中替换普通 3×3 卷积，通过注意力调制频域参数，合成输入自适应的卷积核，从而显式增强方向敏感的高频边界特征。
- **损失函数**：组合 Dice + BCE 的 `L_seg` 与边界损失 `L_bd`（即 `L_S2 = L_seg + β L_bd`）。

## 3. 实验设计
### 主要数据集与场景
- **ISIC-2016**（皮肤镜皮肤病变分割，900/379 训练/验证分割）
- **Kvasir-SEG**（结肠镜息肉分割，880/120 训练/验证分割）
- **GlaS**（组织学腺体分割，85/80 训练/验证分割，标注稀少的典型代表）
- **Synapse 多器官 3D 数据集**（泛化实验，评估对 8 个器官的分割）
- 所有图像统一缩放至 224×224 分辨率。

### 对比方法（Baselines）
- CNN 类：U-Net, U-Net++, Attention U-Net, PraNet, TGANet, DCSAU-Net 等
- Transformer/混合类：XBFormer, CASF-Net, DTAN, DoubleAANet, ConDSeg 等
- 在 Synapse 上还对比了 TransUNet, SwinUNet, MT-UNet, WMREN 等。

### 评估指标
- 区域重叠：mIoU、mDSC（Dice）、Recall、Precision
- 边界距离：HD95（95% 豪斯多夫距离，像素单位）

## 4. 资源与算力
- **硬件**：所有实验在**单块 NVIDIA RTX 4090 GPU** 上完成。
- **训练配置**：
  - Stage‑1：训练 300 epochs，优化器 AdamW，初始学习率 1e-5 线性衰减至 1e-6，全局 batch size 8（配合梯度累积），EMA 衰减率 0.999。
  - Stage‑2：最多 100 epochs，仅微调解码器及编码器最后一个 block，早停；学习率在验证平台期时衰减 0.5，最低 1e-6。
- 论文未明确报告具体训练时长。

## 5. 实验数量与充分性
- **主要对比实验**：在 3 个医学图像数据集上，与 10 余种近年来的先进方法对比，覆盖 CNN 和 Transformer 类模型。
- **消融实验**：
  - 预训练策略消融（无预训练、仅图像均值流、混合域 MAE、混合域均值流、加分散损失）——验证各阶段贡献。
  - 解码器模块消融（标准卷积、CBAM、FDConv、DAF 排列组合）——验证 DAF 与 FDConv 的互补性。
  - 编码器微调策略消融（冻结、全微调、仅最后块微调）。
  - 分散损失权重敏感性分析（λ 从 0.2 到 1.0）。
- **鲁棒性分析**：训练数据量消融（10%~100%）、外观扰动测试（亮度、对比度、模糊、噪声）。
- **可视化分析**：t-SNE 表示分布、DAF 门控图、定性分割对比、频域误差分析。
- **泛化实验**：在 3D Synapse 多器官数据上测试，显示方法对 3D 场景的迁移能力。
- 总体实验设计系统、多层次，对比方法广泛且公平（统一图像尺寸、相同训练/验证划分），消融和鲁棒性分析充分，能支撑核心论断。

## 6. 论文的主要结论与发现
- 纹理偏向是低数据医学图像分割性能下降的关键原因，导致高频边界误差。
- 提出的两阶段框架 **SpectraFlow** 通过**结构感知潜在表示预训练**和**边界敏感的轻量解码器**，显著提升了在低标注条件下的分割质量。
- 在第一阶段，使用`混合域均值流预训练 + 分散损失`能有效学习几何一致且纹理鲁棒的编码器特征，且掩模仅作为结构先验而无语义监督，保证了预训练的任务无关性。
- 第二阶段中，`DAF` 和 `FDConv` 的组合明显地改善了边界刻画精度，并且两者协同作用最佳。
- 方法在多个医学分割基准上（尤其是低数据比例的 ISIC/GlaS）优于当前 SOTA，具有更高的数据效率和更优的抗外观扰动能力。

## 7. 优点
- **创新性强**：首次将均值流传输预训练用于医学图像分割的结构表征学习，并设计掩模作为结构输入而非预测目标，保持了预训练的泛化性。
- **问题定位准确**：深入分析纹理偏差与高频边界错误的关系，并用频域误差图直观佐证。
- **模块设计轻量且有效**：DAF 和 FDConv 均为即插即用，不增加推理开销，却能显著提升边界精度；分散损失同样简单高效。
- **实验严谨**：大量消融、对比和鲁棒性测试，涵盖 2D/3D 不同模态，验证了各个组件的作用和方法的鲁棒性。
- **低数据优势明显**：在极少量标注（10%）下依然保持领先，临床实用价值高。

## 8. 不足与局限
- **对掩模的依赖**：第一阶段预训练需要训练集的二值掩模作为结构输入，意味着仍需一定数量的像素级标注来构造“结构域”，虽不要求分割损失，但在零标注场景下无法应用。
- **3D 实验有限**：仅在 Synapse 一个 3D 数据集上测试泛化性，器官类型和模态覆盖不足，对于三维容积数据的优势和额外开销分析未展开。
- **与大规模预训练模型的比较不够**：虽然以 DINOv2 为 encoder，但没有充分对比使用其他现代基础模型（如 SAM、MedSAM）作为骨干的效果，缺乏在当前“基础模型微调”潮流下的定位。
- **计算开销与训练稳定性讨论较少**：均值流预训练涉及额外的扰动和传输回归，其相较于简单自监督方法的训练时间、显存开销未被量化，且分散损失的理论解释虽在附录中给出，但正文对超参数鲁棒性分析仅有一个子实验（λ 敏感性）。
- **应用场景**：本文以二值分割为主（单个病灶/器官），对于多类、多实例的分割尚未验证，推广应用仍有待探索。

（完）
