---
title: Case-Aware Medical Image Classification with Multimodal Knowledge Graphs and Reliability-Guided Refinement
title_zh: 基于多模态知识图谱和可靠性引导精化的病例感知医学图像分类
authors: "Yiming Xu, Yixuan Liu, Yuhang Zhang, Ling Zheng, Yihan Wang, Qi Song"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.22547v1"
tags: ["query:med-lifelong"]
score: 9.0
evidence: 基于多模态知识图谱的病例感知医学图像分类，实现可解释诊断
tldr: 当前医学图像分类多依赖孤立视觉信息，忽视相似病例与外部知识。本文提出一种病例感知框架，利用多模态知识图谱检索相似病例并构建图结构，通过图注意力网络与跨模态注意力实现知识注入，并结合置信度校准决策细化来抑制噪声。在多数据集上性能显著提升，提供可解释证据，助力临床诊断。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.22547v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 864, \"height\": 738, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22547v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1750, \"height\": 982, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22547v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1820, \"height\": 1078, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22547v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 861, \"height\": 479, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22547v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1570, \"height\": 1363, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.22547v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 876, \"height\": 233, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.22547v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 772, \"height\": 313, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.22547v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1837, \"height\": 1061, \"label\": \"Table\"}]"
motivation: 医学图像分类缺乏对相似病例及关联症状的有效利用，需模拟临床诊断中参考历史病例的过程。
method: 构建多模态知识图谱，检索相似病例并用图注意力网络提取病例特征，通过双向跨模态注意力注入视觉表示，最后用置信度校准决策细化调整贡献。
result: 在多个医学影像数据集上一致超越强基线，消融实验证实各模块的有效性。
conclusion: 该框架通过病例感知推理和可靠性引导细化，提升了分类性能与可解释性，为医学图像诊断提供新范式。
---

## 摘要
深度学习为医学图像分类带来了显著进展，但现有方法大多仍依赖于孤立的视觉证据，无法有效利用相似病例或外部知识。在临床实践中，诊断通常得到历史相似病例及其相关症状的支持。为了模拟这一诊断过程，我们提出了一个框架，利用多模态知识图谱进行病例感知推理，实现可解释的医学图像诊断。给定输入图像，我们的方法从自适应检索的相似病例中构建多模态知识图谱，从而更有效地利用相关样本。我们进一步引入知识传播和注入机制，其中以图像为中心的图注意力网络传播知识语义以获得基于病例的特征，然后通过双向跨模态注意力机制将这些特征注入到视觉表示中，实现跨模态对齐。为了减轻检索噪声，我们设计了一种置信度校准的决策精化方案，通过联合考虑预测置信度和样本相似度来估计每个检索病例的可靠性，自适应地调整其对最终预测的贡献，并提供可解释的病例级别证据。在多个医学影像数据集上的广泛实验表明，我们的方法始终优于强基线，消融研究验证了每个组件的有效性。源代码已公开于 https://anonymous.4open.science/r/MKG-CARE-8B7B。

## Abstract
Deep learning has brought significant progress to medical image classification, yet most existing methods still rely on isolated visual evidence and cannot effectively leverage similar cases or external knowledge. In clinical practice, diagnosis is typically supported by historical similar cases and their associated symptoms. To simulate this diagnostic process, we propose a framework that performs case-aware reasoning using multimodal knowledge graphs for explainable medical image diagnosis. Given an input image, our method constructs a multimodal knowledge graph from adaptively retrieved similar cases, enabling more effective utilization of related samples. We further introduce a knowledge propagation and injection mechanism, where an image-centric Graph Attention Network propagates knowledge semantics to obtain case-based features, followed by a bidirectional cross-modal attention mechanism that injects these features into visual representations for cross-modal alignment. To mitigate noisy retrieval, we design a confidence-calibrated decision refinement scheme that estimates the reliability of each retrieved case by jointly considering prediction confidence and sample similarity, adaptively adjusting its contribution to the final prediction and providing interpretable case-level evidence. Extensive experiments on multiple medical imaging datasets show that our approach consistently outperforms strong baselines, and ablation studies validate the effectiveness of each component. The source code is publicly available at https://anonymous.4open.science/r/MKG-CARE-8B7B.

---

## 论文详细总结（自动生成）

## 一、论文的核心问题与整体含义

- **研究背景**：医学图像诊断是计算机辅助诊断的核心任务，但大多数现有深度学习方法仅依赖当前图像的孤立视觉证据，忽略了历史相似病例及其关联症状的临床参考价值。
- **核心问题**：真实诊断是证据驱动、经验参考的过程，现有方法（纯视觉模型、大视觉语言模型、知识增强模型）存在三大挑战：
  - **挑战一**：缺乏对多模态历史病例的结构化记忆，多数方法将病例作为扁平列表甚至不进行显式检索，无法建模病例间的病理关联。
  - **挑战二**：异质病例知识（连续视觉特征与离散文本语义）难以有效聚合并与查询图像对齐。
  - **挑战三**：检索到的相似病例质量参差不齐，缺少可量化的可靠性评估和可解释的贡献追踪，噪声样本易误导诊断。
- **整体含义**：论文提出一个病例感知框架 MKG‑CARE，通过构建多模态知识图谱、知识传播注入以及置信度校准决策精化，模拟临床“参考历史病例”的过程，实现可解释、鲁棒的医学图像分类。

## 二、方法论

### 2.1 核心思想
- 构建**病例感知分层多模态知识图谱（CH‑MKG）**，将疾病、图像、症状组织为层级结构。
- 通过自适应检索形成查询相关的**星形子图**，利用图注意力网络传播语义，再通过跨模态注意力注入视觉表示，最后用置信度校准抑制检索噪声并输出可解释证据。

### 2.2 关键技术细节
- **CH‑MKG 构建**：  
  - 对每类疾病选择代表性图像作为图像节点，连接对应疾病节点和专家标注的症状节点。  
  - 形成“疾病（顶层）→ 图像（中层）→ 症状（底层）”的层次图，允许自上而下的语义指导和自下而上的细粒度证据。
- **自适应病例检索**：  
  - 用 MedMamba 编码查询图像，在 FAISS 索引中检索相似图像节点。  
  - 计算相似度比率 $u_i = \log(S_i / S_{i+1})$，当超出阈值 $u_{th}$ 时截断列表，动态保留最相关病例。  
  - 对选中图像进行一跳扩展，获取关联症状与疾病节点，形成图像中心的推理子图。
- **知识传播与注入（KPI）**：  
  - **图像中心 GAT 知识增强**：两层图注意力网络（GAT），图像节点用 MedMamba 嵌入初始化，症状/疾病节点用 BioBERT 初始化。通过消息传递，让图像节点吸收医学语义，并抑制噪声症状。聚合检索图像节点的加权表示 $\mathbf{z}_{kg} = \sum \tilde{w}_i \mathbf{h}_i^{(L)}$，权重为归一化相似度。  
  - **双向跨模态对齐**：两个 CrossModalBlock，每个模块先使用知识特征作为 query、视觉特征作为 key/value 进行交叉注意力（知识吸收视觉证据），再反过来更新视觉特征，结合残差连接、LayerNorm 和 MLP。最后融合特征 $\mathbf{h}_{fusion} = \text{concat}(\mathbf{v}_q, \mathbf{z}_{kg}, |\mathbf{v}_q - \mathbf{z}_{kg}|, \mathbf{v}_q \odot \mathbf{z}_{kg})$，送入分类头得到初始预测 $\mathbf{P}_q$。
- **置信度校准决策精化（CDR）**：  
  - 对每个检索到的相似病例，复用其子图并再次前向推理，得到预测分布 $\mathbf{P}_i$。  
  - 定义置信度 $c_i = \max(\mathbf{P}_i)$，综合相似度与置信度计算权重 $w_i \propto (\text{sim}_i)^\alpha \cdot (c_i)^\beta$（默认 $\alpha=\beta=1$）。  
  - 加权聚合得到辅助分布 $\mathbf{P}_{sim}$，最终预测 $\mathbf{P}_{final} = (1-\lambda)\mathbf{P}_q + \lambda \mathbf{P}_{sim}$，既抑制噪声又提供每个病例的贡献度。

## 三、实验设计

- **数据集**：在五个公开医学影像数据集上评估，涵盖不同模态和类别数：  
  - BreastMNIST（病理，2类）  
  - DermaMNIST（皮肤镜，10类）  
  - Kvasir（胃肠内镜，8类）  
  - PAD_UFES_20（皮肤病灶，6类）  
  - RetinaMNIST（眼底图像，5类）  
  所有数据集均存在类别不平衡和细粒度差异。
- **对比基准**：  
  - **视觉模型**：Swin‑Transformer, EfficientNetV2, ConvNeXt, VMamba, MedMamba。  
  - **大视觉语言模型（通用/医学）**：HealthGPT, HuatuoGPT, MedGemma；GPT‑5.1, Gemini‑3‑pro, Qwen3.5‑plus。  
  - **知识增强方法**：KEM（基于 LLM 生成文本知识）。
- **评价指标**：总体准确率（OA）和 Macro‑AUC。
- **实现细节**：图像统一至 224×224，FAISS 索引；GAT 2 层、4 头、维度 768；CrossModalBlock 2 层；融合特征 4×768 维；优化器 AdamW，学习率 5e‑5，batch size 64，最多 40 epochs，早停。

## 四、资源与算力

- 文中明确说明实验在配备**两块 A800 GPU** 的服务器上进行，使用 PyTorch 框架。
- 未提及具体训练时长或总计算量（如 GPU 小时），因此**精确算力消耗未知**。

## 五、实验数量与充分性

- **主要实验**：在 5 个数据集上对比了 12 种以上方法，报告 OA 和 Macro‑AUC，形成全面的性能基线（表 2）。
- **消融实验**：在 Kvasir、RetinaMNIST 上逐步移除 CDR、KPI、CH‑MKG 三个模块，显示各组件贡献（表 3）。
- **参数敏感性分析**：可视化 GAT 层数（0～3 层）对 DermaMNIST 和 Kvasir 的影响；分析融合系数 $\lambda$（0.0～0.5）在 PAD_UFES_20 和 RetinaMNIST 上的趋势（图 5）。
- **定性分析**：提供知识图谱推理路径可视化（图 3）和 Grad‑CAM 注意力对比（图 4），展示可解释性和定位准确性。
- **整体评价**：实验覆盖多种范式对比、组件有效性、参数影响和案例解释，**对比公平、消融充分**。数据集均为公开标准基准，实验设置可复现，但未在外部独立中心数据进行验证。

## 六、主要结论与发现

- MKG‑CARE 在全部五个数据集上均取得最优 OA，Macro‑AUC 在多数数据集领先，显著优于纯视觉、大型多模态模型及知识增强方法。
- 消融证实三个模块均不可或缺：去除 CDR 导致性能下降，去除 KPI 后模型退化为简单相似度聚合，去除 CH‑MKG 中的语义节点进一步伤害性能。
- 可视化显示，模型能精准定位病灶区域，并通过检索相似病例和置信度加权实现决策修正与证据追溯，增强了可解释性。
- 框架轻量且有效，仅依托 MedMamba 基础编码器即可超越数十亿参数的多模态模型，表明结构化知识注入比一味增大模型规模更具优势。

## 七、优点

- **结构创新**：首次将病例感知、多模态知识图谱与可靠性引导精化融入统一框架，显式建模历史病例和症状关联。
- **自适应检索与去噪**：利用相似度梯度动态截断检索，并通过置信度校准自适应地融合检索信息，有效抑制噪声。
- **深度跨模态对齐**：图像中心 GAT + 双向交叉注意力机制，实现视觉特征与文本医学语义的深度融合，而非简单拼接。
- **强可解释性**：可追踪每个检索病例对最终预测的贡献权重，符合临床审计需求。
- **实验设计扎实**：对比基线广泛、涵盖不同范式；消融与参数分析充分；提供多角度定性可视化。
- **资源友好**：在 2 张 A800 上即可完成训练，较 LVLMs 极大降低部署成本。

## 八、不足与局限

- **适用范围有限**：当前仅处理 2D 医学图像，未覆盖 3D 体数据（如 CT、MRI），文中虽提及未来工作，但尚未验证。
- **知识图谱构建依赖专家标注**：症状节点需人工精细标注，对于新疾病或数据匮乏的场景，图谱构建成本高、泛化受限。
- **未报告推理时间**：缺乏与纯视觉模型或 LVLMs 在推理延迟上的对比，实际部署效率不够明朗。
- **跨中心验证缺失**：所有数据集均为公开标准集，未在来自不同医疗机构、不同设备的数据上检验泛化性，可能存在域偏移风险。
- **超参数敏感性**：$\lambda$、$\alpha$、$\beta$ 等参数在不同数据集上最佳值差异明显，可能需要额外调参。
- **图结构简化风险**：仅使用一跳子图和两层 GAT，可能忽略更复杂的病理推理链，对罕见组合症状的建模能力有待验证。

（完）
