---
title: Frequency Adapter with SAM for Generalized Medical Image Segmentation
title_zh: 结合SAM的频率适配器用于泛化医学图像分割
authors: "Phuoc-Nguyen Bui, Van-Nguyen Pham, Duc-Tai Le, Junghyun Bum, Hyunseung Choo"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.09925v1"
tags: ["query:med-lifelong"]
score: 9.0
evidence: 结合频率适配器与SAM的泛化医学图像分割
tldr: 医学图像分割中，域偏移严重阻碍模型跨数据集泛化。现有方法多忽略频率域关键差异。本文提出频率域泛化框架FSAM，结合SAM、LoRA高效微调与频率适配器，提取域不变高频特征以缓解频率域偏移。在眼底和前列腺分割数据集上，FSAM显著超越传统域泛化和基于SAM的方法，验证了频率域泛化的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09925v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1208, \"height\": 1243, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.09925v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1259, \"height\": 914, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.09925v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1256, \"height\": 538, \"label\": \"Table\"}]"
motivation: 解决医学图像分割跨域泛化困难，现有方法忽略频率域差异导致鲁棒性不足。
method: 提出FSAM框架，在SAM中集成LoRA微调和频率适配器，提取域不变高频特征。
result: 在眼底和前列腺数据集上，FSAM域泛化性能优于现有传统和基于SAM的方法。
conclusion: 利用频率域特征增强SAM，能有效缓解医学图像分割中的频率域偏移，提升泛化能力。
---

## 摘要
医学图像分割是计算机辅助诊断和治疗规划中的关键任务。然而，由于成像协议、扫描仪类型和患者群体的差异导致的域偏移，深度学习模型通常难以跨数据集泛化。传统的域泛化方法利用因果特征学习、对抗一致性和风格增强来提高分割的鲁棒性。虽然这些方法有效，但它们依赖于显式特征对齐、对抗目标或手工设计的增强，可能无法充分利用基础模型的能力。最近，分割一切模型（SAM）在分割任务中展现出了强大的泛化能力。基于SAM的域泛化方法试图改进医学图像分割。然而，这些方法主要在空间域中运行，忽视了显著影响模型鲁棒性的频域差异。在这项工作中，我们提出了基于频率的SAM域泛化（FSAM），这是一个新颖的框架，集成了低秩适配（LoRA）以实现高效微调，并采用频率适配器引入频域表示，用于单源域泛化。FSAM通过提取域不变的高频特征来增强SAM的分割鲁棒性，减轻与频率相关的域偏移。在眼底和前列腺数据集上的实验结果表明，FSAM在域泛化方面优于现有的传统域泛化方法和基于SAM的域泛化方法。代码和预训练模型将在GitHub上提供。

## Abstract
Medical image segmentation is a critical task in computer-aided diagnosis and treatment planning. However, deep learning models often struggle to generalize across datasets due to domain shifts arising from variations in imaging protocols, scanner types, and patient populations. Traditional domain generalization (DG) methods utilize causal feature learning, adversarial consistency, and style augmentation to improve segmentation robustness. While effective, these approaches rely on explicit feature alignment, adversarial objectives, or handcrafted augmentations, which may not fully exploit the capabilities of foundation models. Recently, the Segment Anything Model (SAM) has demonstrated strong generalization capabilities in segmentation tasks. SAM-based DG methods attempt to improve medical image segmentation. However, these approaches primarily operate in the spatial domain and overlook frequency-based discrepancies that significantly affect model robustness. In this work, we propose Frequency-based Domain Generalization with SAM (FSAM), a novel framework that integrates Low-Rank Adaptation (LoRA) for efficient fine-tuning and a frequency adapter to incorporate frequency-domain representations for single-source domain generalization. FSAM enhances SAM's segmentation robustness by extracting domain-invariant high-frequency features, mitigating frequency-related domain shifts. Experimental results on fundus and prostate datasets demonstrate that FSAM outperforms existing traditional DG and SAM-based DG approaches in domain generalization. Codes and pre-trained models will be made available on GitHub.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究动机**：医学图像分割中，不同医疗机构间的成像设备、采集协议及患者群体差异会导致域偏移（domain shift），使模型在未见目标域上性能骤降。传统域泛化（DG）方法依赖手工特征对齐或风格增强，未能充分挖掘基础模型的潜力；近日基于分割一切模型（SAM）的方法虽强，却仅在空间域操作，忽视了频率域中的关键差异（纹理、对比度、空间分辨率等引起的频域偏移）。
- **整体含义**：本工作提出 **FSAM（Frequency-based Domain Generalization with SAM）**，通过将频率域适配引入 SAM，提取域不变的高频特征，从而在单源域泛化场景下显著缓解频率相关的域偏移，提升模型在未见域上的分割鲁棒性。

### 2. 论文提出的方法论
- **核心思想**：以冻结的 SAM 图像编码器（ViT‑B）为骨干，利用低秩适配（LoRA）实现轻量微调，同时设计频率适配器（Frequency Adapter）提取、融合频域幅度特征，并用自动化提示生成器（基于 DAPSAM 的记忆原型学习）为 SAM 解码器提供判别性提示。
- **关键技术细节与流程**：
  - **频率输入与适配器**：
    1. 对输入图像做快速傅里叶变换（FFT），提取幅度谱 \( |F(u,v)| \)。
    2. 原始图像与幅度谱分别经过 ViT 的 Patch Embedding，得到图像嵌入和频率嵌入。
    3. 每个频率适配器由线性下投影 → GELU 激活 → 线性上投影构成，共 N 个适配器对应 N 个 ViT 块。每个块输出的图像特征与对应频率适配器的输出逐元素融合，送入下一 ViT 块。
  - **LoRA 微调**：仅对 SAM 编码器中的查询/键/值投影矩阵施加低秩更新 \( \Delta W = AB \)，秩 \( r=4 \)，大幅减少可训参数，同时保留预训练知识。
  - **自动提示生成器**（沿用 DAPSAM 设计）：
    1. 对编码器输出做全局平均/最大池化得到实例原型。
    2. 通过可学习的记忆矩阵 \( M \) 进行相似度检索与加权聚合，生成精炼原型。
    3. 计算精炼原型与编码器输出的余弦相似度作为激活图。
    4. 将精炼原型、编码器嵌入和激活图拼接后经 \( 1\times1 \) 卷积生成最终提示。
  - **训练目标**：混合损失 \( \mathcal{L} = (1-\lambda)\mathcal{L}_{CE} + \lambda\mathcal{L}_{Dice} \)，\( \lambda = 0.8 \)。
- **公式化要点**：
  - FFT：\( F(u,v) = \sum_{h,w} f(h,w) e^{-j2\pi (\frac{uh}{H}+\frac{vw}{W})} \)
  - LoRA：\( W' = W + AB \)，其中 \( A\in \mathbb{R}^{d\times r}, B\in \mathbb{R}^{r\times d} \)，\( r\ll d \)。

### 3. 实验设计
- **数据集与场景**：
  - **RIGA+ 眼底数据集**：5 个不同域（BinRushed、Magrabia、Messidor Base 1/2/3）。分别以 BinRushed 和 Magrabia 作为源域进行训练，在剩余三个域上测试，评估视盘（OD）和视杯（OC）分割的 Dice 系数。
  - **前列腺 MRI 数据集**：6 个域（A–F），采用留一法（leave‑one‑domain‑out）评估：每次用 1 个域训练（90% 训练，10% 域内验证），在其余 5 个域上测试 Dice。
- **对比方法**：
  - 传统 DG 方法（CNN 骨干）：CSDG、ADS、MaxStyle、D‑Norm、SLAug、CCSDG、AdvBias、RandConv、MixStyle。
  - 基于 SAM 的方法：SAMed、DeSAM（box/points 提示）、MedSAM、DAPSAM。
- **实现细节**：输入尺寸分别为 512×512（眼底）和 384×384（前列腺）；优化器 AdamW，初始学习率 5e‑4，权重衰减 0.1；预热 250/25 epochs，最大训练 200 epochs，早停于 160 epochs；LoRA 秩为 4。

### 4. 资源与算力
- 文中未明确说明 GPU 型号、数量、训练时长等算力信息。仅提及使用 ViT‑B 版 SAM，并给出超参数配置，未报告硬件开销或推理速度。

### 5. 实验数量与充分性
- **主要对比实验**：在 2 个数据集、共 2（RIGA+ 源域选择）×3 目标域 + 6（前列腺留一法）×5 目标域 = 约 36 种测试场景下，与 11 种以上的传统及 SAM 方法进行了全面比较，实验规模较大，覆盖了常见的单源域泛化基准。
- **消融实验**：文中未见专门的消融分析（如单独移除频率适配器、LoRA、不同频带选择等），这是实验设计的一个空白。
- **公平性**：所有方法采用相同的源域/目标域划分、评价指标和预处理流程，对比条件控制良好。未提及多轮随机种子或统计检验，但结果呈现趋于稳健。

### 6. 论文的主要结论与发现
- FSAM 在所有测试场景下均显著优于传统 DG 方法（如 CSDG、CCSDG）和基于 SAM 的方法（如 DAPSAM）。
- 在 RIGA+ 上，以 Magrabia 为源域时，视盘 Dice 达 96.39%，视杯达 88.63%，相对最优 CCSDG 分别提升 1.41% 和 3.10%。
- 在前列腺数据集上，FSAM 平均 Dice 为 82.74%，比最佳 DAPSAM 提升 1.43%，对比传统 DG 提升达 12.68%。
- 频率适配器能够有效提取域不变的高频信息，结合 LoRA 微调，使 SAM 这样的基础模型在医学图像跨域分割中鲁棒性更强。

### 7. 优点
- **新颖的频率域融合**：首次将频域适配器嵌入 SAM 的 ViT 层级，显式建模并缓解频率相关域偏移。
- **参数高效**：通过 LoRA 仅训练少量参数，保留了 SAM 的泛化能力，同时降低微调成本。
- **与提示学习协同**：结合记忆原型自动生成提示，进一步增强了未见域的适应性。
- **多数据集验证**：在眼底彩色图像和前列腺 MRI 两种不同模态、多域的基准上均展示出稳定提升，证明方法的通用性。

### 8. 不足与局限
- **缺少消融研究**：未定量验证频率适配器、LoRA、不同频带（如仅低频/高频）的单独贡献，难以明确增益来源。
- **实验覆盖有限**：仅涉及两个数据集，缺少其他模态（如 CT、超声）和更多病灶类型的评估；也未讨论在极低资源或噪声场景下的表现。
- **潜在偏差风险**：单源域泛化基准的域划分和预处理可能引入选择偏差；未报告多次运行的均值和方差，结果的稳定性证据不足。
- **应用限制**：未分析计算开销和推理速度，实际部署可行性未知；对频域特征依赖的理论解释较为初步，可能不适用于域偏移不以频率为主的场景。
- **隐私与公平性**：未讨论患者隐私、数据去偏等问题。
（完）
