---
title: Context-driven Missing-Modality Learning for Robust Medical Diagnosis with Image-Tabular Data
title_zh: 基于上下文驱动的缺失模态学习，实现图像-表格数据的鲁棒医学诊断
authors: "Tianling Liu, Lequan Yu, Tong Han, Liang Wan"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25968v1"
tags: ["query:med-lifelong"]
score: 9.0
evidence: 在模态缺失情况下利用多模态影像和临床数据进行鲁棒医学诊断
tldr: "在临床实践中，多模态图像与表格数据对精准诊断至关重要，但模态任意缺失严重影响模型性能，现有丢弃或合成方法面临信息丢失与跨模态依赖捕捉不足的挑战。本文提出上下文驱动的缺失模态学习框架 CMML，设计级联残差变压器自编码器，以可学习上下文令牌作为语义先验，协同记忆库合成关键缺失表示；随后通过实例自适应语义参考实现异质表示对齐，并采用类别感知对比学习提取鉴别性特征。在 Derm7pt、ODIR 和 MEN 三个数据集上，CMML 显著优于 SOTA 方法，平均 AUC 分别提升 1.26%、0.97% 和 1.32%，验证了其在任意缺失条件下鲁棒诊断的有效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25968v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1798, \"height\": 849, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25968v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 857, \"height\": 347, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25968v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 863, \"height\": 477, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25968v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 871, \"height\": 446, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25968v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 869, \"height\": 544, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25968v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1804, \"height\": 395, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25968v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 800, \"height\": 338, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25968v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 876, \"height\": 465, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.25968v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 733, \"height\": 368, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25968v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1792, \"height\": 738, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25968v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 818, \"height\": 254, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25968v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1803, \"height\": 297, \"label\": \"Table\"}]"
motivation: 临床中图像或表格数据常缺失，现有丢弃或合成方法信息损失且缺乏跨模态语义建模。
method: 提出CMML框架，用级联变压器和记忆库合成缺失模态，再通过实例自适应语义参考对齐及类别对比优化。
result: "在三个医疗数据集上，CMML平均AUC分别提升1.26%、0.97%和1.32%，超越现有方法。"
conclusion: CMML通过上下文先验与语义对齐有效应对任意模态缺失，为鲁棒多模态诊断提供新方案。
---

## 摘要
整合多种影像和临床表格记录的多模态数据对于准确医学诊断至关重要，但特定模态的任意缺失在临床实践中普遍存在，严重降低了多模态模型的性能。现有方法要么丢弃缺失模态导致信息丢失，要么在未捕捉复杂模态间依赖关系的情况下难以合成缺失模态。为解决这些局限，我们提出了一种新颖的上下文驱动的缺失模态学习框架，通过顺序执行模态合成与语义对齐，实现在任意缺失条件下的鲁棒诊断。具体而言，我们设计了一种基于级联残差变换器的自编码器，利用可学习的上下文令牌作为数据集级语义先验，捕捉模态间依赖关系并合成关键的缺失表征，这些表征进一步由模态特定的记忆库丰富。为解决原始可用表征与合成表征之间的差异，我们通过注入来自 CRTA 输出的多模态表征，将学习到的上下文令牌转换为实例自适应的语义参考。该参考引导异质模态表征对齐到统一空间，最终应用类别感知对比精炼来探索判别性诊断线索。在皮肤病变、眼病和脑膜瘤数据集上的大量评估表明，CMML 显著优于现有最先进方法，AUC 平均改进分别为 1.26%、0.97% 和 1.32%。

## Abstract
While multimodal data integrating diverse imaging and clinical tabular records is crucial for accurate medical diagnosis, the arbitrary absence of specific modalities is prevalent in clinical practice, severely degrading the performance of multimodal models. Existing methods either discard missing modalities, leading to information loss, or struggle to synthesize them without capturing complex inter-modal dependencies. To address these limitations, we propose a novel Context-driven Missing-Modality Learning (CMML) framework, which sequentially performs modality synthesis and semantic alignment to achieve robust diagnosis under arbitrary missing conditions. Specifically, we design a Cascade Residual Transformer-based Autoencoder (CRTA) that leverages learnable context tokens acting as dataset-level semantic prior to capture inter-modal dependencies and synthesize key missing representations. These representations are further enriched by modality-specific memory banks. To resolve the discrepancy between original available and synthesized representations, we transform the learned context tokens into instance-adaptive semantic references by infusing multimodal representations from the CRTA's outputs. This reference guides the alignment of heterogeneous modality representations into a unified space, where class-aware contrastive refinement is finally applied to explore discriminative diagnostic cues. Extensive evaluations on skin lesion (Derm7pt), ocular disease (ODIR), and meningioma (MEN) datasets demonstrate that CMML significantly outperforms state-of-the-art (SOTA) methods, yielding AVG AUC improvements of 1.26%, 0.97%, and 1.32%, respectively.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将基于您提供的论文内容，生成一份结构化、深入、客观的中文总结。

### 1. 论文的核心问题与整体含义

*   **核心问题**：在利用多模态数据（影像和表格数据）进行临床诊断时，模态的任意缺失是一个普遍且严峻的挑战。现有模型在模态缺失情况下性能会严重下降，可能导致误诊。
*   **研究动机**：
    *   现有处理模态缺失的方法主要分为两类，但都存在局限性。
    *   **联合表征学习**：仅利用可用模态，忽略了缺失模态中潜在的判别性信息，导致性能次优。
    *   **多模态信息补全**：在输入或表征层面合成缺失信息。输入级重建计算开销大且引入噪声；表征级补全则难以在复杂的任意缺失模式下捕捉精细的模态间依赖关系，且合成表征与原始表征之间存在显著的语义差距，直接融合会产生语义干扰。
*   **整体含义**：本文旨在解决在任意模态缺失条件下，如何鲁棒地利用多模态数据进行精准诊断的问题。核心思想是不仅要“合成”缺失模态，更要“对齐”合成与原始的异质表征，从而确保最终融合特征既具有语义一致性，又能保留关键的诊断信息。

### 2. 论文提出的方法论

论文提出了一个名为“上下文驱动的缺失模态学习”的创新框架，该方法遵循“先合成，后对齐”的策略。

#### **核心思想**
1.  **合成阶段**：利用数据集级别的语义先验（`上下文令牌`）和模态特定的细节信息（`记忆库`），为缺失模态生成高质量的表征。
2.  **对齐与精炼阶段**：将上述先验进化为“实例自适应语义参考”，引导所有表征对齐到统一空间，消除语义鸿沟，并通过对比学习增强类别可分离性。

#### **关键技术细节**

*   **上下文驱动的模态补全模块**：
    *   **动态缺失嵌入初始化**：当某模态缺失时，其初始表征不是静态向量，而是通过交叉注意力（Cross-Attention）从所有可用模态中动态查询得到，使其初步具备当前实例的语义先验。
    *   **级联残差变换器自编码器**：
        *   采用一系列具有编码器-解码器瓶颈结构的Transformer块，逐级恢复缺失表征。
        *   引入**可学习的上下文令牌**，与所有模态令牌拼接后输入。在自注意力机制中，这些上下文令牌作为**数据集级语义先验**，从所有可用模态令牌中聚合信息，捕捉全局的模态间依赖关系，从而指导缺失模态的合成。
        *   **公式化流程**：输出第 `l` 个自编码器 `Φ_l` 的结果。首次输入 `f`（上下文令牌+模态令牌），后续输入 `f` 与之前所有输出的和。
    *   **记忆驱动的表征丰富**：
        *   `记忆更新`：在`可用`模态下，采用移动平均策略，用该模态的令牌更新与其最相似的记忆条目。
        *   `记忆检索`：在`缺失`模态下，根据合成的基表征从对应的记忆库中通过软注意力检索并融合模态特异性信息，得到最终的补全表征 。
    *   **合成监督**：使用余弦相似性损失来约束合成表征与真实表征之间的相似度。

*   **实例自适应的上下文引导对齐与精炼模块**：
    *   **实例自适应的上下文引导多模态对齐**：
        *   `参考构建`：将自编码器输出的精炼多模态表征注入到数据集级上下文令牌中，将其转化为**实例自适应的语义参考** ，使其能代表当前特定病例的多模态语义共识。
        *   `表征对齐`：以实例自适应的语义参考为查询，通过交叉注意力机制，将来自不同模态的所有表征（包括合成的和原始的）投影到一个统一的语义空间，得到对齐后的表征。
    *   **类别感知的判别性精炼策略**：
        *   将对齐后的多模态表征拼接并进行全局平均池化，得到融合的多模态特征。
        *   采用双重对比学习增强判别性：
            *   **样本级对比学习**：以单一样本为锚点，其正例为同类别的原型，负例为其他类别的原型。
            *   **原型级对比学习**：以类别原型为锚点，其正例为同类别下的所有样本，负例为其他类别的原型。
    *   **最终目标函数**：总体损失函数由分类交叉熵损失、合成相似性损失、样本级对比损失和原型级对比损失加权求和构成。

### 3. 实验设计

*   **数据集与场景**：
    *   **Derm7pt（皮肤病变）**：827例，包含临床图像、皮肤镜图像和5项表格属性，二分类（黑素瘤 vs 痣）。
    *   **ODIR（眼部疾病）**：2641例，包含左右眼眼底图像和2项表格属性，三分类（正常、糖尿病、其他）。
    *   **MEN（脑膜瘤，内部数据集）**：796例，包含T1C、FLAIR-C、ADC三种MRI序列和6项表格属性，三分类（G1, G2inv, G2ninv）。此数据集用于测试模型可扩展性。
    *   **场景**：模拟了所有可能的任意模态缺失组合进行测试。

*   **Benchmark与对比方法**：
    *   **联合表征学习方法**：ShaSpec, SFusion, SimMLM。
    *   **多模态信息补全方法**：Redcore (表征级), MCMoE (表征级)。

*   **评价指标**：
    *   准确率和AUC（曲线下面积）。
    *   对性能提升进行了威尔科克森符号秩检验，以验证统计显著性（p-value < 0.05）。

### 4. 资源与算力

*   **硬件配置**：所有实验均在**单块NVIDIA GeForce RTX 3090 GPU**上完成。
*   **训练时长与框架**：论文未明确提及具体的总训练时长。模型使用PyTorch框架实现，所有数据集均训练**50个epoch**。
*   **模型复杂度**：论文给出了参数量和计算量的对比，CMML在确保高性能的同时，其参数量和计算量（FLOPs）相较于部分SOTA方法保持了较低或相当的水平，例如在Derm7pt上参数量为30.71M，计算量为8.96 GFLOPs。

### 5. 实验数量与充分性

*   **实验组数与类型**：
    *   **主实验**：在三个特性各异的数据集上，与**5种SOTA方法**在**所有可能的模态缺失组合**下进行了全面对比。例如，Derm7pt有7种组合，MEN有15种组合。
    *   **消融实验**：在两个数据集上进行了详细的消融研究，验证了CMML框架中各个关键模块的有效性，包括：
        *   `CMC模块`中的记忆库、上下文令牌。
        *   `ICAR模块`中的ICMA、CDR，以及上下文令牌从“数据集级”到“实例级”的进化。
    *   **可视化分析**：
        *   **t-SNE**：对比了不同消融设置下，合成表征与可用表征的类分布，直观展示了合成特征的质量。
        *   **MDA**：可视化了融合特征的潜在空间，展示了CMML比SOTA方法拥有更紧凑的类内距和更大的类间距。
    *   **参数敏感性分析**：系统分析了记忆库更新轮次、记忆库令牌数量以及上下文令牌数量（与总模态令牌的比例）等关键超参数对性能的影响。
*   **充分性与公平性**：实验设计非常充分和客观。
    *   **充分性**：覆盖了多种疾病、模态组合、对比方法和消融分析，多角度的可视化也为方法有效性提供了强有力的定性支撑。
    *   **公平性**：所有对比方法都使用了相同的骨干网络和特征提取器，确保了公平比较。统计显著性检验也增加了结果的可信度。

### 6. 论文的主要结论与发现

*   **性能优越性**：提出的CMML框架在所有三个数据集的几乎所有任意缺失场景下，都显著优于现有的SOTA方法，在平均AUC上分别取得了1.26%（Derm7pt）、0.97%（ODIR）和1.32%（MEN）的提升。
*   **模块有效性**：CMC模块中的上下文令牌和记忆库，以及ICAR模块中的实例自适应对齐和对比精炼，均对最终性能有正向贡献，证明了“合成后对齐”策略的正确性。
*   **可扩展性**：CMML在拥有4个模态的MEN数据集上的优异表现，证明了其中心化（hub-centric）的架构能够灵活处理不同数量的模态，无需针对性地调整结构，具备良好的可扩展性。
*   **特征质量提升**：可视化和分析结果证实，CMML合成出的缺失模态表征具有更好的类别区分性，且对齐后的融合特征在潜在空间中展现出更优的聚类特性。

### 7. 优点

*   **方法创新性强**：
    *   **上下文令牌的进化**：巧妙地让上下文令牌在“合成阶段”作为数据集级先验，在“对齐阶段”进化为实例自适应语义参考，构思精巧，是连接两个核心模块的桥梁。
    *   **记忆库的设计**：通过记忆更新和检索机制，有效弥补了合成表征中缺失的模态特异性细节。
*   **框架灵活可扩展**：以上下文令牌为中心的设计，使其天然能够处理任意数量的模态和复杂的缺失模式，而无需改变网络结构。
*   **实验设计扎实、全面**：
    *   对比方法多样，涵盖两大主流范式。
    *   评估全面，测试了所有可能的模态缺失组合。
    *   消融和可视化分析丰富，对提出的每个创新点都进行了剥离验证，结论令人信服。

### 8. 不足与局限

*   **依赖完整训练集**：该框架的训练依赖于所有模态齐全的完整数据集，这限制了其对现实世界中大量存在部分缺失的海量临床数据的利用。这是论文作者也明确指出的一大局限。
*   **模态不平衡问题**：论文作者在讨论中提到，当缺失的巧合为“主要模态”时，合成特征可能无法完全替代其关键诊断信息。这也是该方法面临的一个挑战。
*   **表格数据处理**：表格数据被转换为文本描述后使用预训练的LLM（DeepSeek-R1）提取特征。这种处理方式的效果可能依赖于预训练模型的医学知识水平和模板设计，其泛化性值得进一步探讨。
*   **内部数据集的可复现性**：MEN作为私有数据集，其结果的可复现性受到一定限制。

（完）
