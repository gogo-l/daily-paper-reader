---
title: "Chaos-SSL: An Attention-Based Self-Supervised Learning Framework with Chaotic Transformation for Medical Image Classification"
title_zh: Chaos-SSL：基于注意力机制的自监督学习框架与混沌变换用于医学图像分类
authors: Joao Batista Florindo
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.27146v1"
tags: ["query:med-lifelong"]
score: 9.0
evidence: 利用混沌变换进行医学图像分类的自监督学习框架
tldr: 医学图像分类依赖大量标注，而标准自监督学习（SSL）的简单数据增强无法捕捉细粒度病理纹理。本文提出Chaos-SSL框架，利用一维混沌映射生成复杂增强样本进行对比学习，并引入注意力融合机制动态结合混沌模型与ImageNet预训练模型的特征。在ISIC 2018和APTOS 2019数据集上，该方法准确率分别达0.9261和0.8726，显著超越现有SSL方法，为无标注医学图像分类提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27146v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1665, \"height\": 1329, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27146v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 824, \"height\": 929, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27146v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 684, \"height\": 941, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27146v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 680, \"height\": 536, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.27146v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 679, \"height\": 410, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.27146v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 829, \"height\": 231, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.27146v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1140, \"height\": 539, \"label\": \"Table\"}]"
motivation: 标准SSL的简单几何、颜色增强难以捕获医学图像中细微病理的复杂纹理，限制了分类性能。
method: 提出两阶段Chaos-SSL：第一阶段用混沌映射增强进行对比学习预训练，第二阶段通过注意力融合结合混沌模型与通用预训练模型的特征。
result: 帐篷映射预训练30 epoch后融合，在ISIC 2018和APTOS 2019上准确率分别达0.9261和0.8726，大幅领先现有SSL方法。
conclusion: Chaos-SSL通过混沌变换增强自监督学习，有效捕获细粒度纹理，显著提升医学图像分类，减少对标注的依赖。
---

## 摘要
自监督学习（SSL）已成为减轻对大规模标注数据集依赖的强大范式，而标注数据的缺乏是医学图像分析中的常见瓶颈。然而，标准的SSL方法依赖于简单的几何和颜色增强，可能无法捕捉分类细微病理所需的细粒度、复杂纹理细节。本文介绍了Chaos-SSL，一种新颖的两阶段医学图像分类框架。在第一阶段，我们提出了一种新的自监督预训练策略，利用一维混沌映射（Logistic、Tent和Sine）作为对比学习的复杂非线性增强。我们假设这些混沌变换能创建“更难”且语义更丰富的视图，迫使网络学习医学纹理的鲁棒表示。在第二阶段，我们引入了一种基于注意力的融合模型，该模型动态地将Chaos-SSL模型的专业特征与更大的ImageNet预训练模型的通用特征相结合。我们在两个公开数据集上验证了我们的方法：ISIC 2018（皮肤病变）和APTOS 2019（糖尿病视网膜病变）。结果表明，使用Tent映射预训练30个epoch的Chaos-SSL模型，再结合注意力融合，性能与最先进水平完全媲美，在ISIC 2018上准确率为0.9261，在APTOS 2019上为0.8726。这显著优于现有的SSL方法，包括几种最新方法。

## Abstract
Self-Supervised Learning (SSL) has emerged as a powerful paradigm to mitigate the reliance on large, annotated datasets, a common bottleneck in medical image analysis. However, standard SSL methods, which rely on simple geometric and color augmentations, may fail to capture the fine-grained, complex textural details necessary for classifying subtle pathologies. This paper introduces Chaos-SSL, a novel two-stage framework for medical image classification. In the first stage, we propose a new self-supervised pre-training strategy that leverages 1D chaotic maps (Logistic, Tent, and Sine) as a complex, non-linear augmentation for contrastive learning. We hypothesize that these chaotic transformations create ``harder'' and more semantically-rich views, forcing a network to learn robust representations of fine-grained medical textures. In the second stage, we introduce an attention-based fusion model that dynamically combines the specialized features from our Chaos-SSL model with the general-purpose features of a larger, ImageNet-pre-trained model. We validate our method on two public datasets: ISIC 2018 (skin lesions) and APTOS 2019 (diabetic retinopathy). Our results demonstrate that the Chaos-SSL model pre-trained with a Tent map for 30 epochs, followed by attention fusion, achieves performance fully competitive with the state-of-the-art, yielding an accuracy of 0.9261 on ISIC 2018 and 0.8726 on APTOS 2019. This significantly outperforms existing SSL methods, including several recent approaches.

---

## 论文详细总结（自动生成）

# Chaos-SSL 论文结构化总结

## 1. 研究背景与核心问题
- **背景**：深度学习在医学图像分析上取得卓越成果，但高度依赖大规模高质量标注数据；医学领域标注成本高、耗时长、需要专业知识，形成“数据稀缺”瓶颈。
- **现有问题**：自监督学习（SSL），尤其对比学习，通过无标签数据预训练缓解数据稀缺。但标准增强方法（随机裁剪、翻转、颜色抖动等）旨在让模型对简单几何/颜色变化不变，难以捕捉医学图像中细微病理所依赖的细粒度、复杂纹理信息。
- **研究动机**：能否通过复杂度更高、非线性、确定性的混沌映射作为增强手段，迫使网络学习更鲁棒、更具纹理不变性的表征？并在此基础上，将专门纹理特征与通用特征动态融合，提升分类性能。

## 2. 方法论
### 整体框架（Chaos-SSL）
两阶段加一融合的三步流程：
1. **Stage 1: 混沌增强对比预训练（Chaos-SSL）**
   - **混沌变换（ChaoticTransform）**：对归一化到 [0,1] 的图像逐像素施加 1D 混沌映射迭代，迭代次数 `k ~ U[1,5]`。实现三种映射：
     - Logistic 映射 `f(x) = 3.99 x(1-x)`
     - Tent 映射 `f(x) = 2.0 min(x,1-x)`
     - Sine 映射 `f(x) = 1.0 sin(πx)`
   - **视图生成**：标准视图 `v1 = T_std(I)`；混沌视图 `v_chaos = T_chaos(T_std(I))`，混沌变换在标准增强之后应用。
   - **对比学习框架**：采用 SimCLR 风格，编码器 E（ConvNeXt-Tiny）+ 投影头 P（3 层 MLP，输出 128 维），计算 NT-Xent 对比损失。
   - **训练设置**：编码器学习率 10⁻⁷，投影头学习率 10⁻³，AdamW 优化器，预训练 15 或 30 epoch；之后丢弃投影头，保存编码器权重。

2. **Stage 2: 有监督微调基础模型**
   - **Model 1（ImageNet-Large）**：ConvNeXt-Large 基于 ImageNet 预训练权重，用目标数据集微调 20 epoch，余弦退火学习率。
   - **Model 2（Chaos-SSL-Tiny）**：加载 Stage 1 编码器权重，接新线性分类头，微调 10 epoch。

3. **Stage 3: 基于注意力的特征融合**
   - 提取两个骨干网特征 `f1, f2`，拼接后经 Squeeze-and-Excite 注意力机制：
     - `w_attn = σ(W2 · ReLU(W1 · f_concat))`
   - 元素乘得到 `f_attended`
   - 最终分类器输出 logits
   - 训练时骨干网学习率 10⁻⁶，融合头部学习率 10⁻⁴，训练 10 epoch。

### 公式与核心思想
- 对比损失：NT-Xent 公式，温度参数 τ。
- 注意力融合：学习自动加权两大特征流的贡献，兼顾全局上下文与局部纹理。

## 3. 实验设计
- **数据集**：
  - ISIC 2018：10,015 张皮肤镜图像，7 类病变。
  - APTOS 2019：3,662 张眼底图像，5 级糖尿病视网膜病变严重程度。
- **基准方法**（来自文献对比）：
  - CE（交叉熵监督基线）
  - CL（对比学习，Marrakchi 等 2021）
  - ProCo（原型感知对比学习，Yang 等 2022）
  - FG-SSL（细粒度自监督 Jigsaw 预训练，Park & Ryu 2024）
- **内部消融对比**：
  - 不同混沌映射（Logistic, Sine, Tent）
  - 不同 SSL 预训练 epoch（15 vs 30）
  - 不同损失函数（与 FG-SSL 中的 KL-div、Barlow Twins 对比）
- **评估指标**：准确率（Accuracy）与宏平均 F1 分数（Macro F1）。

## 4. 资源与算力
- **硬件**：使用单块 NVIDIA RTX 5080 GPU。
- **训练时间/数量**：未明确给出总训练时长，但给出了各阶段 epoch 数（SSL 预训练 15 或 30，监督微调 10 或 20，融合 10）。参数量方面，ConvNeXt-Tiny 和 ConvNeXt-Large 均有标准规模，融合模型推理需运行两个骨干网。

## 5. 实验数量与充分性
- **实验组数**：
  - APTOS 2019：6 组（15/30 epoch × 3 种映射）
  - ISIC 2018：6 组
  - 与文献对比：1 组最优配置（30 epoch Tent）与 4 种前人方法对比
  - 损失函数消融：1 组（与 FG-SSL 内部两种损失对比）
- **实验充分性**：
  - 在两个常用医疗影像数据集上进行了较全面的映射类型和 epoch 数量消融。
  - 与近期 SOTA 方法直接对比，结果列于相同表格之中，具备一定公平性。
  - 但未进行多次重复实验以建立误差范围，也未与其他最新 SSL 架构（如 MAE、DINO）直接比较。
- **客观性**：对比方法的数据均直接引用原论文，避免了信息偏倚；但研究中未提及超参数搜索的细节。

## 6. 主要结论与发现
- **混沌增强有效性**：Tent 映射预训练 30 epoch 效果最佳，能显著提升细粒度医学图像分类性能。
- **注意力融合增益**：融合 Chaos-SSL-Tiny 的纹理专项特征与 ImageNet-Large 的全局语境特征，实现性能互补，超越单独模型。
- **SOTA 性能**：
  - APTOS 2019：准确率 0.8726，F1 0.7601（超越 FG-SSL 1.7% / 5.6 个百分点）
  - ISIC 2018：准确率 0.9261，F1 0.8706（超越 FG-SSL 3.2% / 6.7 个百分点）
- **机理推测**：混沌变换创建“更困难”的正样本对，迫使编码器学习对复杂非线性纹理扭曲不变的表示；Tent 映射的分段线性特性可能保持边缘结构，使其成为理想的“硬”增强。

## 7. 优点
- **创新性强**：首次将混沌映射用作 SSL 数据增强，开辟了混沌理论与对比学习交叉的新方向。
- **方法合理**：将“硬”增强与注意力融合巧妙结合，形成从预训练到任务迁移的完整流水线。
- **实验扎实**：两个医学图像基准数据集、多组消融（映射类型、训练时长），并与近年 SOTA 直接对比，结论可信。
- **性能显著**：在两个数据集上均大幅领先对比方法，具有实际应用潜力。

## 8. 不足与局限
- **数据集覆盖范围窄**：仅测试于 2D 单模态（皮肤镜、眼底照片），未在 3D（CT、MRI）或其他模态（组织病理等）上验证。
- **计算开销**：推理需同时运行两个骨干网（Large + Tiny），增加延迟，不利于实时临床部署。
- **可解释性有限**：Tent 映射为何最优仍属经验性结论，缺乏严格的理论分析或关于混沌属性与特征不变性的深度解释。
- **实验重复性未体现**：未报告多次运行的标准差或置信区间，难以判断结果的稳定性。
- **对比范围可扩展**：未与最新非对比 SSL（如 MAE、DINOv2）直接比较，也缺少与其他复杂增强方法的直接对比。

（完）
