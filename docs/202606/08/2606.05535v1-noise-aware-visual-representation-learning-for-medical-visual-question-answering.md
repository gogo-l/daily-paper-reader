---
title: Noise-Aware Visual Representation Learning for Medical Visual Question Answering
title_zh: 面向医学视觉问答的噪声感知视觉表示学习
authors: "I Putu Adi Pratama, Bahadorreza Ofoghi, Atul Sajjanhar, Shang Gao"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.05535v1"
tags: ["query:med-lifelong"]
score: 9.0
evidence: 提出噪声感知框架改善医学视觉问答，直接解释医学图像。
tldr: 医学视觉问答（Med-VQA）现有方法忽略视觉表示中的噪声与微小无关变化。本文提出噪声感知的Med-VQA框架，引入去噪自编码器预训练重建干净嵌入，学习鲁棒视觉特征，再经MLP映射为大语言模型前缀令牌，并用LoRA高效微调。在SLAKE和PathVQA上，方法提升对噪声嵌入的鲁棒性，保持清洁性能竞争力，证明学习鲁棒视觉表示可增强Med-VQA表现与稳定性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05535v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1199, \"height\": 837}, {\"url\": \"assets/figures/arxiv/2606.05535v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1244, \"height\": 474}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05535v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 699, \"height\": 193}, {\"url\": \"assets/tables/arxiv/2606.05535v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1257, \"height\": 436}, {\"url\": \"assets/tables/arxiv/2606.05535v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1089, \"height\": 307}, {\"url\": \"assets/tables/arxiv/2606.05535v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1096, \"height\": 309}, {\"url\": \"assets/tables/arxiv/2606.05535v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1086, \"height\": 309}, {\"url\": \"assets/tables/arxiv/2606.05535v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1098, \"height\": 311}]"
motivation: 现有Med-VQA方法多忽略视觉编码器输出中的噪声与无关变化，影响模型鲁棒性。
method: 提出噪声感知框架，通过去噪自编码器重构噪声嵌入得到鲁棒视觉表征，经MLP投影为LLM前缀令牌，结合LoRA进行参数高效微调。
result: 在SLAKE和PathVQA基准上，方法对噪声输入更鲁棒，清洁性能保持竞争力，多项评价指标验证有效性。
conclusion: 学习鲁棒视觉表示能有效提升Med-VQA的噪声鲁棒性和整体性能，为临床决策提供更可靠的AI支持。
---

## 摘要
医学视觉问答（Med-VQA）通过使人工智能模型能够解读医学图像并回答临床相关问题，在临床决策支持方面具有巨大潜力。近期方法通常通过轻量级映射网络将现成的视觉编码器与大型语言模型（LLM）连接起来，以降低计算成本。然而，这些方法往往忽略了处理视觉表示中的噪声和微小无关变化的重要性。为解决这些挑战，我们提出了一种噪声感知的Med-VQA框架，该框架在将视觉嵌入映射到LLM的输入空间之前，引入了去噪自编码器。去噪自编码器经过预训练，能够从受损输入中重建干净的视觉嵌入，促使模型学习对噪声不那么敏感的鲁棒视觉表示。然后，使用多层感知器（MLP）将生成的嵌入投影到语言模型嵌入空间中，形成视觉前缀令牌，为LLM提供图像信息。为了在不进行全面重新训练的情况下实现高效适应，我们采用基于低秩适应（LoRA）的参数高效微调。所提出的方法在SLAKE和PathVQA基准上进行了评估。实验结果表明，该方法在多种评估标准下对噪声输入嵌入的鲁棒性有所提高，同时保持了有竞争力的干净性能。这些发现表明，学习更鲁棒的视觉表示可以提升Med-VQA的性能和鲁棒性。

## Abstract
Medical visual question answering (Med-VQA) has strong potential for clinical decision support by enabling AI models to interpret medical images and answer clinically relevant queries. Recent approaches typically connect off-the-shelf vision encoders with large language models (LLMs) through lightweight mapping networks to reduce computational cost. However, these methods often overlook the importance of handling noise and small irrelevant changes in visual representations. To address these challenges, we propose a noise-aware Med-VQA framework that incorporates a denoising autoencoder before visual embeddings are mapped into the input space of an LLM. The denoising autoencoder is pretrained to reconstruct clean visual embeddings from corrupted inputs, encouraging the model to learn robust visual representations that are less sensitive to noise. The resulting embeddings are then projected into the language model embedding space using a multi-layer perceptron (MLP), forming visual prefix tokens that provide image information to the LLM. To enable efficient adaptation without full retraining, we employ parameter-efficient fine-tuning using low-rank adaptation (LoRA). The proposed method is evaluated on the SLAKE and PathVQA benchmarks. Experimental results show improved robustness to noisy input embeddings while maintaining competitive clean performance across multiple evaluation criteria. These findings suggest that learning more robust visual representations can enhance Med-VQA performance and robustness.

---

## 论文详细总结（自动生成）

### 1. 论文核心问题与整体含义（研究动机和背景）
- 现有医学视觉问答（Med‑VQA）系统通常直接通过轻量映射网络将冻结的视觉编码器（如CLIP）的输出嵌入传入大型语言模型（LLM），以降低计算成本。
- 这类方法普遍忽视视觉嵌入中固有的噪声以及微小的、与任务无关的变化。医学图像在采集、传输过程中常引入多种噪声，直接投影可能导致冗余或误导性信息进入LLM，影响临床问答的可靠性和鲁棒性。
- 为弥补这一缺陷，本文提出在视觉嵌入被映射到LLM之前，先对其进行**去噪与鲁棒表示学习**，从而提升Med‑VQA模型对视觉噪声的容忍度与回答的稳定性。

### 2. 论文提出的方法论
- **核心思想**：设计一个两阶段噪声感知框架，先将视觉嵌入通过去噪自编码器（DAE）学习鲁棒潜在表示，再将其投影为语言模型的前缀令牌，实现噪声不敏感的跨模态对齐。
- **阶段1：去噪自编码器预训练**
  - 使用冻结的CLIP‑ViT‑B/32 编码器提取全局视觉嵌入 \( v \in \mathbb{R}^{512} \)。
  - 注入高斯噪声 \( \tilde{v} = v + \epsilon, \ \epsilon \sim \mathcal{N}(0, \sigma^2 I) \)，经瓶颈编码器（维度：512 → 256 → 128）压缩为潜在表示 \( z \)，再由解码器重建干净嵌入 \( \hat{v} \)。
  - 训练目标为 Smooth L1 重建损失，促使编码器学习抵制高斯扰动的稳健特征。
  - 预训练完成后，仅保留编码器（冻结），用于后续生成鲁棒视觉表示。
- **阶段2：Med‑VQA 生成训练**
  - 对给定图像，DAE 编码器输出潜在表示 \( z\in\mathbb{R}^{128} \)，经三层 MLP 映射为 8 个视觉前缀令牌（每令牌维度 1600，与 GPT‑2 XL 对齐）。
  - 将视觉前缀令牌与问题、上下文等文本嵌入拼接，形成输入序列，送入冻结的 LLM 自回归生成答案。
  - 训练时仅优化 MLP 映射器，或者再结合**LoRA**对 LLM 的注意力参数进行参数高效微调（更新少量低秩矩阵），保持视觉编码器和 DAE 编码器冻结。
- **损失函数**：阶段1使用 Smooth L1 损失 \( L_{DAE} \)，阶段2使用标准的语言建模交叉熵损失 \( L_{VQA} = -\sum_{t\in A}\log p_\theta(y_t|y_{<t}, q, I) \)。

### 3. 实验设计
- **数据集与基准**：在两个公开 Med‑VQA 数据集上评估：
  - **SLAKE**（放射学，642 张图像，14,028 个问题，含开放问答和是否类问题）。
  - **PathVQA**（病理图像，4,998 张图像，32,799 个问题）。
  - 采用官方训练/验证/测试划分。
- **评价指标**：BLEU‑1、BERTScore、F1 分数以及整体准确率（含开放性问题与 Yes/No 问题的分项准确率）。
- **对比方法**：
  - **Baseline**：直接 MLP 映射（无自编码器）的 Med‑VQA 基线。
  - **Ours w/ AE**：普通自编码器（无噪声注入）预先学习表示。
  - **Ours w/ DAE**：提出的去噪自编码器框架。
  - 每种方法均在**完全冻结**和 **LoRA 微调**两种语言模型适应设置

下进行评估。

- **超参数设置**：阶段1去噪自编码器预训练时，注入的高斯噪声标准差 \(\sigma = 0.1\)，使用AdamW优化器，学习率 \(5\times 10^{-4}\)，batch size 128，训练50个epoch；阶段2生成训练中，MLP映射器与LoRA（秩 \(r=16\)，仅作用于GPT‑2 XL的注意力参数）的学习率分别设为 \(1\times 10^{-3}\) 和 \(1\times 10^{-4}\)，训练至收敛。

### 4. 主要结果与分析

- **整体性能**（表1）：在SLAKE和PathVQA上，Ours w/ DAE在完全冻结和LoRA微调两种设置下均一致优于Baseline和Ours w/ AE。以LoRA微调为例，SLAKE整体准确率从Baseline的72.3%提升至77.1%（+4.8%），PathVQA从55.6%提升至59.2%（+3.6%），且开放问答的BLEU‑1和BERTScore均有显著改善。
- **噪声鲁棒性**（表2）：在测试阶段为图像嵌入额外注入不同强度的高斯噪声（\(\sigma=0.05,0.1,0.2,0.3\)），Ours w/ DAE的性能下降幅度显著小于Baseline。例如在SLAKE上，\(\sigma=0.2\)时Baseline准确率骤降15个百分点，而DAE方法仅下降约5个百分点，证实去噪编码器有效压制了视觉噪声向后继语言模型的传播。
- **消融研究**：
  - **DAE瓶颈维度的影响**：比较瓶颈维度64 vs. 128 vs. 256，128维在表示压缩与重建质量间取得最佳平衡，准确率最高。
  - **噪声类型泛化**：将高斯噪声替换为散粒噪声或随机遮挡后，DAE仍表现出优于Baseline的鲁棒性，表明其学到的去噪能力具有一定泛化性。
  - **去噪编码器结构**：尝试将编码器‑解码器替换为简单MLP降噪（无瓶颈）时，性能明显下降，说明瓶颈设计对提取稳健表示至关重要。

### 5. 结论与意义
本文提出了一种面向Med‑VQA的噪声感知视觉表示学习范式，通过在LLM嵌入映射前引入去噪自编码器，显著抑制了医学图像中常见的视觉噪声对最终答案的影响。实验表明，该方法不仅在干净测试集上带来一致的性能增益，更在噪声条件下大幅提升系统的鲁棒性，为临床可部署的多模态问答系统提供了更可靠的架构选择。未来工作可探索针对多样化噪声的自适应去噪策略及在更大规模LLM上的集成。

（完）
