---
title: Foundation-Preserving Adaptation via Generalized Rayleigh-Quotient Optimization
title_zh: 基于广义瑞利商优化的基础保持适应
authors: "Dongjun Kim, Adrian de Wynter, Huancheng Chen, Heasung Kim, Haris Vikalo"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2606.00132v1"
tags: ["query:continual"]
score: 9.0
evidence: FoLoRA通过瑞利商评分更新方向，平衡任务效用与旧知识保留，防止遗忘
tldr: 基础模型在微调适应下游任务时，往往会损害预训练阶段习得的通用能力，导致非目标能力的灾难性遗忘。现有遗忘感知方法通常依赖特殊初始化或固定约束，难以在训练过程中动态平衡适应性与保持性。本文提出FoLoRA框架，利用一阶保持条件，分别基于预训练代理激活和下游激活定义遗忘惩罚与任务效用，并通过广义瑞利商评估更新方向单位惩罚下的效用，构建谱坐标系。在此坐标系中，采用方向门控Adam优化，衰减低效用-惩罚比的方向，从而在优化目标任务的同时维护预训练能力。实验表明，在数学推理、代码生成和指令遵循等任务上，FoLoRA不仅提升了目标性能，更在非目标能力保持方面全面优于现有基线，实现了适应与保持的最佳折衷，为安全高效的基础模型适应提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00132v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1647, \"height\": 395, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00132v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1641, \"height\": 385, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00132v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1640, \"height\": 399, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.00132v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1642, \"height\": 443, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00132v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1638, \"height\": 372, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00132v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1564, \"height\": 417, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00132v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1650, \"height\": 482, \"label\": \"Table\"}]"
motivation: 微调基础模型常损害预训练通用能力，现有遗忘感知方法未能在训练中动态权衡适应性与保持性。
method: FoLoRA利用一阶保持条件，基于代理激活定义遗忘惩罚与任务效用，通过广义瑞利商量化方向比值，实现方向门控Adam更新，衰减低效用-惩罚比。
result: 在数学、代码、指令遵循任务上，FoLoRA在提升目标性能的同时，非目标能力保持全面优于基线，达到最佳适应-保持平衡。
conclusion: FoLoRA为平衡适应与遗忘提供了新优化框架，显著优于现有方法，实现了安全高效的基础模型适应。
---

## 摘要
尽管微调能有效地将基础模型适应于专门的下游任务，但它可能会降低预训练期间获得的非目标能力。现有的遗忘感知方法通常通过专门的初始化或固定约束来寻求更安全的更新，但并未在训练过程中调节适应与保持之间的权衡。我们提出了基础保持低秩适应（FoLoRA），一个遗忘感知的优化框架。在一阶保持条件的指导下，FoLoRA 针对预训练代理激活定义了遗忘惩罚，并针对下游任务激活定义了任务效用。然后，它通过广义瑞利商，根据单位遗忘惩罚的任务效用来对更新方向进行评分。由此产生的谱坐标系使得能够进行方向门控的 Adam 更新，在训练过程中衰减低效用惩罚的方向。为了估计遗忘惩罚，FoLoRA 通过从预训练模型采样来构建预训练代理校准数据，而不是依赖于单一的代理数据集。在数学、代码和指令遵循适应上的实验表明，FoLoRA 在保持与适应之间实现了最强的平衡，在提升目标任务性能的同时，最佳地聚合保持了非目标能力。

## Abstract
While finetuning effectively adapts foundation models to specialized downstream tasks, it can degrade nontarget capabilities acquired during pretraining. Existing forgetting aware methods typically seek safer updates through specialized initialization or fixed constraints, but do not regulate the adaptation preservation trade-off during training. We propose Foundation Preserving LoRA (FoLoRA), a forgetting aware optimization framework. Guided by a first order preservation condition, FoLoRA defines a forgetting penalty over pretraining-proxy activations and a task utility over downstream task activations. It then scores update directions by task utility per unit forgetting penalty via a generalized Rayleigh quotient. The resulting spectral coordinate system enables direction wise gated Adam updates, attenuating low utility to penalty directions during training. To estimate the forgetting penalty, FoLoRA constructs pretraining proxy calibration data by sampling from the pretrained model rather than relying on a single proxy dataset. Experiments on math, code, and instruction following adaptation show that FoLoRA achieves the strongest preservation adaptation balance over baselines, improving target task performance with best aggregate preservation of non target capabilities.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **核心问题**：基础模型（如大语言模型LLM）在下游任务（如数学推理、代码生成）上微调时，常出现**灾难性遗忘**现象——目标任务性能提升的同时，预训练习得的广泛非目标能力（如世界知识、阅读理解、事实推理等）会显著下降。
- **研究动机**：现有缓解遗忘的方法（如MiLoRA、CorDA、LoRA-Null）多依赖于**一次性初始化**或**固定投影约束**，并未在训练过程中动态权衡“适应（提升目标任务）”与“保持（保留预训练知识）”。
- **整体含义**：本文提出 **FoLoRA (Foundation-Preserving LoRA)**，一个**遗忘感知的优化框架**，通过在训练期间持续为每个更新方向评分（任务效用 vs. 遗忘风险），实现方向级动态调控，从而在提升下游任务性能的同时，最大限度地保持预训练能力。

### 2. 方法论核心思想与技术细节

- **一阶保持条件与方向评分（广义瑞利商）**
  - 从**一阶泰勒展开**出发，推导出：保持模型在预训练数据上激活行为不变，等价于约束LoRA更新（∆A）在预训练数据激活特征（$X_{pre}$）上的扰动范数。
  - 基于此，定义了两个度量：
    - **遗忘惩罚 $R_{pre}(\Delta A)$**：$||\Delta A X_{pre}||^2_F$（正定化后为 $Tr(\Delta A H \Delta A^T)$）。
    - **任务效用 $U_{task}(\Delta A)$**：$||\Delta A X_{task}||^2_F$（$=Tr(\Delta A C_{task} \Delta A^T)$）。
  - 核心创新：通过**广义瑞利商 $\rho(v) = \frac{v^T C_{task} v}{v^T H v}$**，为每个候选更新方向 $v$ 计算**单位遗忘代价下的任务效用**。
  - 最大化该商等价于求解广义特征值问题 $C_{task} v = \mu H v$，得到一组 **$H$-正交的广义特征基**（谱坐标系），其对应的广义特征值 $\gamma_i$ 即方向评分：$\gamma_i$ 大则表示该方向高效且安全，$\gamma_i$ 小则表示方向低效且风险高。

- **谱门控Adam（SAdam）优化器**
  - 将LoRA的下投影矩阵 $A$ 的梯度变换至该**谱坐标系**（使遗忘惩罚白化，任务效用对角化）。
  - 计算Adam的动量（$m_t, v_t$）后，在进行参数更新前，对每个谱方向施加**单调门控**：$s_i = \frac{\gamma_i}{\gamma_i + \lambda} \in [0,1)$。
  - 更新公式修正为 $\Delta A_t = -\eta \frac{\hat{m}_t}{\sqrt{\hat{v}_t} + \epsilon} \odot \max(s, s_{min})$，再映射回原始参数空间。
  - **效果**：保留高分方向的全量更新，大幅衰减低分方向的更新幅度，在整训练过程中**动态**调控适应-保持平衡。

- **模型生成式预训练代理校准**
  - 无需原始预训练语料，直接从**预训练模型本身采样生成文本**，构建代理数据集 $S_{MG}$。
  - 用该代理数据计算 $X_{pre}$，以估计遗忘协方差矩阵 $C_{pre}$。
  - 理论支撑：**定理3.1**证明，在一定条件下，模型生成的分布与预训练分布之间的KL散度可被有效控制。

### 3. 实验设计

- **基础模型与任务**
  - 主实验：**LLaMA2-7B**。
  - 扩展实验：**Qwen3-1.7B**，并使用不同大小的Qwen3模型（0.6B, 1.7B, 4B）生成校准数据。
  - **下游适应任务三类**：数学推理（MetaMathQA）、代码生成（CodeFeedback）、指令遵循（WizardLM-Evol-Instruct）。

- **Benchmark与评价指标**
  - **保持能力（非目标能力）**：
    - 核心集：TriviaQA， NQ Open, WebQS（开放域事实检索）。
    - 拓展集（仅Qwen3实验）：IFEval（指令遵循）， TruthfulQA（幻觉鲁棒性）， SQuAD-v2（阅读理解）， MMLU（多任务知识）， BABILong & LongBench-v2（长文本）， GPQA（科学推理）。
  - **适应能力（目标任务）**：GSM8K & MATH（数学）， HumanEval & MBPP（代码）， IFEVAL（指令遵循）。
  - **综合指标**：$Avg1$（保持分均值）， $Avg1(\%)$（保持率）， $Avg2$（任务分均值）， $GM$（保持与任务的几何平均）。

- **对比基线**
  - 标准LoRA、**MiLoRA**、**CorDA**、**LoRA-Null**、**OPLoRA**，以及未微调的基座模型。

### 4. 资源与算力

- **硬件**：所有实验均在**单个NVIDIA GH200 120GB GPU**上运行。
- **训练设置（所有方法公平一致）**：
  - 优化器：AdamW，学习率 $2\times10^{-5}$，批次大小128，bf16精度。
  - 数据量：每个目标数据集的前10万次对话，训练1个epoch。
  - LoRA秩：$r=128$。
- **额外开销（以Qwen3-1.7B为例）**：
  - **训练时**：比标准LoRA**时间增加仅1.6%**，**峰值显存增加14.8%**。
  - **一次性预处理**：校准数据生成、协方差矩阵构建及广义特征分解的总耗时约2.4分，显存开销小于1000MB，占比极小。

### 5. 实验数量与充分性

- **实验矩阵覆盖广泛**：
  - **3个不同的适应任务**（数学、代码、指令遵循）。
  - **2个基础模型家族**（LLaMA2， Qwen3）。
  - **多基线对比**：与5种以上代表方法进行对比。
- **充分且深入的消融研究**：
  - **超参数**：详细分析了控制保守程度的 $\lambda$ （图2b）和 $s_{min}$（图2c）对保持-适应平衡的影响，展示了其光滑可控的帕累托曲线（图2d）。
  - **校准数据有效性**：通过互换校准数据源（NQ Open vs. 模型生成）并应用于FoLoRA、CorDA、LoRA-Null，验证了**模型生成校准数据的优越性**（表2， 图3a）。还通过PCA可视化展示了不同数据源的分布覆盖范围（图3b）。
- **公平性与客观性**：
  - 所有方法在**统一的训练与评估协议**下复现，排除了训练细节差异的干扰。
  - 不仅依赖单一事实检索基准衡量保持能力，还引入了**全面的多维度保持能力测试集**。
  - 提供了详尽的开销分析（图3c/d），论证方法的高效性。

### 6. 主要结论与发现

- **综合性能领先**：FoLoRA在所有三个主要适应任务上，均取得了**最强的保持能力（$Avg1$）**和**最优的适应-保持综合平衡（$GM$）**。在数学和代码任务上，其保持率（$Avg1(\%)$）甚至**超过了基座模型**。
- **动态调控的有效性**：实验表明，通过调整超参数 $\lambda$ 和 $s_{min}$，FoLoRA可以平滑地控制更新策略的保守程度，沿着比所有对比方法更优的适应-保持边界移动。
- **校准数据的关键作用**：
  - 用大规模的模型生成数据校准，比使用传统的单个基准数据集（如NQ Open）提供了**更广的分布覆盖和更强的全领域保持能力**。
  - 生成校准数据的模型容量越大，最终FoLoRA的综合保持性能通常越好。
- **精细化遗忘模式**：实验揭示，微调对不同非目标能力的遗忘程度并不均匀，对事实检索、MMLU等任务影响显著，而对指令遵循、长文本等能力影响较小。

### 7. 优点

- **方法论创新**：
  - **首次**将适应-保持权衡建模为广义瑞利商问题，为每个更新方向赋予了明确的效用/代价评分。
  - 提出了**谱门控Adam（SAdam）**，将静态的方向选择改为**训练全程的动态、软性调控**，相比于约束投影或固定初始化，更为灵活和精细。
- **实验设计严谨**：
  - **多维度的保持能力评估**，远超以往工作仅关注问答准确率的局限性。
  - **全面的对比与消融**，明确了方法优势来源于“谱坐标系”和“动态门控”，并对模型生成校准数据的价值进行了令人信服的验证。
  - 提供了详实的**时间与显存开销分析**，证明方法高效可行。
- **实用性强**：**模型生成式校准**解决了实际应用中难以访问预训练原始数据的问题，显著降低了方法的应用门槛。

### 8. 不足与局限

- **适应任务局限性**：所有实验均在标准的监督微调（SFT）任务上完成，未探讨该方法在强化学习人类反馈（RLHF）、偏好对齐（如DPO）等更复杂的后训练范式下的表现。
- **额外显存开销**：尽管开销相对小，但在大模型训练显存普遍紧张的背景下，约15%的显存增长对于极限规模模型或资源受限场景仍构成一定应用壁垒。
- **理论假设**：理论分析部分（如定理3.1）依赖于Transformer具有通用逼近性的假设，但在实际使用的、有限规模的模型中，该假设是否严格成立有待考证。
- **模型生成校准的潜在偏差**：模型生成的数据分布可能放大了预训练模型已存在的偏差，或将特定模式进行过度强化，其对适应后安全性的影响未被评估。

（完）
