---
title: Domain-Agnostic Incremental Learning for Sound Classification. A DCASE 2026 Challenge task
title_zh: 声源分类的领域无关增量学习：一项DCASE 2026挑战任务
authors: "Riccardo Casciotti, Manjunath Mulimani, Manu Harju, Jesper Rindom Jensen, Annamaria Mesaros"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.02173v1"
tags: ["query:continual"]
score: 9.0
evidence: 提出声音分类的领域增量学习任务，要求顺序学习不遗忘且无法访问先前数据
tldr: "针对声音分类中模型跨域遗忘问题，DCASE 2026首次提出域无关增量学习挑战，要求系统在三个声学域顺序学习十类声音且禁止访问旧数据。基线模型仅获44.9%跨域平均准确率，主要因域推断错误。该任务为跨域持续学习建立了公开评估基准，促进领域发展。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.02173v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 786, \"height\": 612, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02173v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 645, \"height\": 578, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.02173v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 643, \"height\": 228, \"label\": \"Table\"}]"
motivation: 传统增量学习因声学域变化导致灾难性遗忘，亟需域无关持续学习方案。
method: 定义三域顺序学习任务，要求模型不访问旧数据，学习十类声音。
result: "基线系统跨域平均准确率仅44.9%，主要错误源于域推断失误。"
conclusion: 为域无关增量学习建立首个公开竞赛基准，揭示现有模型局限。
---

## 摘要
本文介绍了DCASE 2026挑战中的音频分类任务——领域无关增量学习。增量学习指的是在同一系统上顺序学习新任务，同时保持其对先前学习任务的知识和性能。声源分类的领域增量学习是指在不同的声学领域中学习相同的声源类别，这在DCASE 2026中首次被形式化为一项数据挑战。参与者将训练一个系统在三个不同领域中学习十种声源类别，每个增量任务的学习过程中无法访问先前任务的数据。提交的系统将根据在三个领域上计算的整体平均准确率进行排名。所提供的基线系统在三个领域上获得了44.9%的适中准确率，这主要是由于对测试样本所属领域的错误推断所致。

## Abstract
This paper presents the Domain-Agnostic Incremental Learning for Audio Classification Task of the DCASE 2026 Challenge. Incremental learning refers to sequentially learning new tasks with the same system while maintaining its knowledge and performance on the previously learned task. Domain-incremental learning for sound classification refers to learning the same sound classes but in different acoustic domains, and was formalized as a data challenge for the first time in DCASE 2026. Participants will train a system to learn ten sound classes in three different domains, with learning at each incremental task not having access to previous task data. Submitted systems will be ranked by the overall average accuracy calculated over the three domains. The provided baseline system obtains a modest performance of 44.9\% accuracy over the three domains, mostly due to erroneous inference of the domain for the test sample.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究动机与背景**  
  - 实际声音分类系统需要顺序学习不同声学域（如不同录制场景、设备）中的同一组声源类别，但传统方法在跨域学习时易发生灾难性遗忘（即学会新域后完全忘记旧域知识）。  
  - 现有的域适应通常仅涉及源域与目标域两个域，且保留源域数据；而域增量学习（DIL）则要求在无法访问旧域数据的前提下，逐步学习多个域并维持所有已见域的性能。  
  - 本文首次将**域无关增量学习（DAIL）** 形式化为 DACE 2026 挑战的正式任务：系统必须在不获取测试样本域标签的情况下，依次学习三个不同声学域中的十类相同声音事件（如警报、狗叫、引擎等），且每个阶段只能使用当前域的训练数据。

- **任务的整体含义**  
  - 该挑战旨在推动长期知识保持与跨域泛化能力的研究，通过统一评估框架与数据集为持续学习社区提供一个标准基准，评估模型在无域身份线索时的增量分类性能。

### 2. 论文提出的方法论
- **任务设定与规则**  
  - 增量学习分三个阶段：首先系统在域 D1 上预训练（参与者不直接获得 D1 数据，但基线模型已内嵌 D1 知识），随后依次在 D2 和 D3 上增量学习，每个阶段只提供当前域的数据。  
  - 不允许使用外部数据或预训练模型生成嵌入，仅可使用任务提供的开发集并允许数据增强（如音高偏移、时间拉伸）。  
  - 推理时，系统不获得任何域标签，必须将任意输入音频分类到十个预定义类别之一。

- **评价指标**  
  - 整体平均准确率：先分别计算每个域的类别平均准确率，再对三个域的域平均准确率取均值，从而平衡各域样本量不均的影响。

- **基线系统（关键技术细节）**  
  - **架构**：采用类 CNN14 的六卷积块结构，每块含两层卷积与一层批归一化（BN）。全局池化后接入带 softmax 的分类器。  
  - **音频表示**：所有音频重采样至 32 kHz，切割为 4 秒片段；对片段提取 64 维对数梅尔能量谱（50 Hz–14 kHz，1024 点汉明窗，跳跃步长 320 点）。  
  - **域适应策略**：引入域专属 BN 层。对每个新域 D，在共享卷积参数基础上仅调整对应的 BN 层参数，以适应域特有的数据分布。  
  - **推理时的域选择**：为每个输入音频分别通过共享层与各域专属 BN 层的组合进行前向计算，得到多个类概率分布；计算每个分布的熵，选择熵最小（不确定性最低）的域专属层决定最终预测类别。  
  - **训练细节**：初始训练 120 轮，批量大小 32，Adam 优化器，初始学习率 1e‑4，增量阶段学习率降至 1e‑5。

### 3. 实验设计
- **数据集**  
  - 开发集：包含域 D2（139 分钟音频）和 D3（275 分钟音频），已划分训练/测试子集。域 D1 的数据不公开，但基线系统已用 D1 训练，参与者从已含 D1 知识的初始模型开始。  
  - 评估集：包含 D1、D2、D3 三个域的音频，但仅提供音频文件，不提供域标注。

- **对比/基准**  
  - 主要报告基线系统的域无关表现（基于熵域选择）。  
  - 为分析上限，文中提供了任务依赖（oracle）版本：在测试时使用正确的域标签选择对应 BN 层，此时 D2 准确率达 71.8%，D3 达 63.4%，跨域平均 67.6%。该对比用于指出域预测错误是性能瓶颈。

- **评估场景**  
  - 基线结果仅针对 D2 和 D3 在开发测试集上给出（表1）；D1 的性能将在评估集发布后补充。

### 4. 资源与算力
- **算力说明**  
  - 文中仅提及训练超参数（120 epochs，batch size 32，Adam 优化器，学习率阶梯），**未提供 GPU 型号、数量或具体训练时长**。从任务规模（音频总时长约 414 分钟，4 秒片段，小模型）推测，单张普通 GPU 即可快速完成，但缺乏明确记录。

### 5. 实验数量与充分性
- **实验体量**  
  - 目前只展示了单一基线系统在开发集的两个域上的测试结果，并比较了域无关与域已知两种推理模式。  
  - 未包含消融研究（如不同 BN 层数目、不同域选择策略的对比）或其他持续学习方法（如重放、正则化、动态架构）的对比。

- **实验充分性评价**  
  - 作为挑战任务描述而非完整研究论文，实验量极其有限，主要目的是确立任务框架和基准性能。  
  - 由于评估集尚未发布，最终的跨域完整结果、排行榜分析以及成功方法的特点尚未呈现，因此当前实验在严谨对比和全面性上尚不充分。但基准设定客观透明，规则公平（禁止外部数据、统一评估指标）。

### 6. 论文的主要结论与发现
- **基线性能差，域推断是关键瓶颈**  
  - 领域无关推理时，基线系统在 D2 上准确率 54.7%，学习 D3 后在 D3 上仅 35.0%，跨域平均 44.9%。  
  - 当采用 oracle 域标签时，性能提升至平均 67.6%，表明基线的主要失效原因在于域特定 BN 层的选择错误——熵估计偏向于 D1，导致大量 D2/D3 样本被误判为 D1。

- **任务价值**  
  - 域无关增量学习在声音分类中具有高度实用性与挑战性，需要更先进的域身份预测或鲁棒的表征学习方法。  
  - 本次挑战为这一方向提供了首个标准化基准，有望促进持续学习技术的进步。

### 7. 优点
- **任务定义新颖且贴近现实**：首次将域无关增量学习形式化为公开挑战，要求模型在无域标签下分类，比域已知设定更具挑战性和实用意义。  
- **评估指标合理**：采用域平均准确率，避免因数据量失衡导致大域主导排名。  
- **严格规则保障公平**：明确禁止外部数据、预训练模型及对评估集的主观标注，鼓励方法创新。  
- **基线清晰，分析透彻**：不仅给出域无关结果，还通过 oracle 对比指出性能瓶颈，为后续研究指明了方向（提高域推断准确性）。  
- **数据增强开放**：允许音频变换，为参与者提供了合理的灵活性。

### 8. 不足与局限
- **数据集规模与多样性有限**：仅三个域，总音频量较小（约 414 分钟），且域来源未公开，可能限制泛化性研究。  
- **开发集不包含 D1**：参与者无法直接评估对首域知识的遗忘程度，也不利于设计防遗忘策略。  
- **基线方法简单**：仅依赖 BN 层调整和熵选择进行域适应，未展示更先进的持续学习技术（如重放、知识蒸馏、动态扩展）。  
- **实验覆盖不足**：缺乏消融研究、多方法对比及统计显著性检验，当前结果仅具参考性而非常规研究论文的完备性。  
- **无计算成本报告**：未提供 GPU 型号、训练时间等资源消耗，难以评估不同方案的实用效率。  
- **结果阶段尚不完整**：挑战赛结果与分析尚未纳入，当前论文更像前景规划而非结论性研究。

（完）
