---
title: "CheXanatomy: Anatomy-Aware Vision-Language Modeling for Chest Radiographs"
title_zh: CheXanatomy：面向胸部X光片的解剖感知视觉-语言建模
authors: "Sergios Gatidis, Curtis Langlotz, Christian Bluethgen"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08420v1"
tags: ["query:med-lifelong"]
score: 9.0
evidence: 解剖感知的胸部X光视觉语言模型
tldr: 为弥补预训练视觉语言模型缺乏细粒度解剖结构的不足，CheXanatomy通过自回归令牌预测微调模型生成解剖分割掩膜，利用CT合成胸部X光片提供监督。该方法无需任务专用解码器，实现了解剖感知的胸部X光理解，在下游分割任务中表现优异，为零样本医学图像分割提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08420v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1266, \"height\": 825, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08420v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1263, \"height\": 595, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08420v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1245, \"height\": 334, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08420v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1247, \"height\": 856, \"label\": \"Figure\"}]"
motivation: 解剖感知的胸部X光视觉语言模型。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
在大规模图像-文本对上预训练的视觉-语言模型（VLM）表现出强大的图像级理解能力，但主要针对全局对齐进行优化，并未明确编码细粒度的解剖结构，这限制了其在分割等空间精确任务中的适用性。我们提出了CheXanatomy，一个通过自回归令牌空间监督将显式解剖学知识整合到预训练VLM中的框架。该模型不是添加任务特定的解码器头，而是通过下一个令牌预测来生成解剖分割掩码。为了实现可扩展的监督，我们从CT体积合成逼真的胸部X光片，并将CT分割标签前向投影以获得解剖一致的二维掩码。我们在合成和真实胸部X光片上针对U-Net基线评估了该方法，包括在模型规模、输入分辨率和视觉编码器微调方面的消融研究。自回归解剖监督在分布内实现了与专用卷积模型相当的性能，并在域迁移到真实CXR数据时展现出改进的几何鲁棒性。此外，解剖预训练模型在有限监督下适应新定位任务时表现出更高的样本效率。更大的模型和更高的输入图像分辨率提高了性能，而视觉编码器微调效果有限。这些结果表明，将解剖结构直接嵌入生成目标可促进空间锚定的表示，并支持解剖感知的医学视觉-语言建模。

## Abstract
Vision-language models (VLMs) pretrained on large-scale image-text pairs demonstrate strong image-level understanding, but are primarily optimized for global alignment and do not explicitly encode fine-grained anatomical structure, limiting their suitability for spatially precise tasks such as segmentation. We introduce CheXanatomy, a framework that integrates explicit anatomical knowledge into a pretrained VLM through autoregressive token-space supervision. Instead of adding task-specific decoder heads, the model is trained to generate anatomical segmentation masks via next-token prediction. To enable scalable supervision, we synthesize realistic chest radiographs from CT volumes and forward-project CT segmentation labels to obtain anatomically consistent 2D masks. We evaluate the approach on synthetic and real chest radiographs against a U-Net baseline, including ablations on model scale, input resolution, and vision encoder fine-tuning. Autoregressive anatomical supervision achieves performance comparable to specialized convolutional models in-distribution and demonstrates improved geometric robustness under domain shift to real CXR data. In addition, anatomy-pretrained models exhibit improved sample efficiency when adapting to novel localization tasks under limited supervision. Larger models and higher input image resolution improve performance, while vision encoder fine-tuning has limited effect. These results show that embedding anatomical structure directly into the generative objective promotes spatially grounded representations and supports anatomy-aware medical vision-language modeling.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **核心问题**：现有预训练视觉-语言模型（VLM）主要优化全局图像-文本对齐，缺少对细粒度解剖结构的显式编码，难以胜任胸片（CXR）中需要精确空间定位的任务（如多结构分割）。
- **整体含义**：将解剖知识直接嵌入 VLM 的生成目标中，使模型在保持语言交互能力的同时获得解剖感知，而不依赖额外的像素级解码器。

### 2. 方法论
- **基础架构**：基于预训练 VLM PaliGemma 2，包含 SigLIP 视觉编码器、线性投影层与 Gemma 2 语言模型，原生支持输出边界框与分割掩码（掩码经 VQ-VAE 离散化为 16 个 token）。
- **自回归解剖监督**：将分割任务完全转化为下一 token 预测。模型接收图像和提示（如“分割心脏”），自回归生成目标解剖结构的定位边界框（4 个专用 token）与掩码 token 序列，再由 VQ-VAE 解码器恢复二元掩码。损失仅为标准交叉熵，**无像素级损失或额外分割头**。
- **可扩展的合成监督**：利用 CT-RATE 数据集中的 CT 扫描，通过 TotalSegmentator 获取 80 类解剖标签，并用可微分 DRR（Diff-DRR）分别生成后前位和侧位合成 X 光片及对应的投影掩码。训练时引入随机缩放变换增强尺寸与采集几何的变异性。
- **高效微调**：使用 LoRA 在 8 张 H100 GPU 上进行参数高效微调，冻结大部分预训练权重，只调整少量参数。

### 3. 实验设计
- **数据集与场景**：
  - **合成分布内测试**：CT-RATE 的 1,366 张投影图像（后前位和侧位）。
  - **真实域迁移测试**：
    - CheXmask（200 张后前位胸片，含心脏与肺掩码）。
    - VinDr-RibCXR（198 张后前位胸片，含双侧肋骨掩码）。
  - **新任务少样本迁移**：RadVLM 视觉定位数据集（检测肺动脉增宽、左侧膈肌等），使用 0%–100% 训练子集评估样本效率。
- **评估指标**：Dice、IoU、Hausdorff 距离、傅里叶描述子距离（FDD）、边界框 IoU。
- **对比方法**：
  - **专用分割基线**：输入分辨率 448×448 的 2D U-Net，使用相同合成数据训练。
  - **VLM 变体消融**：模型规模（3B vs 10B）、输入分辨率（224×224 vs 448×448）、视觉编码器冻结与否。

### 4. 资源与算力
- **硬件**：训练在 8 块 H100 GPU 上完成。
- **批次大小**：根据配置调整，10B 模型 448×448 分辨率为 batch size 8，3B 模型 224×224 为 batch size 24。
- **总训练时长**：论文**未明确给出**总的训练小时数或 epoch 数，仅说明了批次大小和硬件配置。

### 5. 实验数量与充分性
- **主要实验**：覆盖 3 个测试集（1 个合成、2 个真实）上的分割评估，加上 RadVLM 的 6 个数据比例（0%–100%）的少样本迁移实验。
- **消融研究**：系统比较 2 种模型规模 × 2 种输入分辨率 × 2 种视觉编码器状态（冻结/微调），共 8 种 VLM 配置与 U-Net 对比。
- **实验充分性**：实验设计较为完整，覆盖分布内、域迁移、样本效率三个维度，消融因子明确，对比基线典型。但缺少与其他先进 VLM 分割方法（如 SAM‑based 或 CLIP‑guided 解码器方案）的直接比较，仅以 U‑Net 作为唯一卷积基线，公平性受限于基线选取范围。

### 6. 主要结论与发现
- **分布内性能**：10B + 448×448 的 CheXanatomy 在 Dice 和 IoU 上与专用 U‑Net 接近，但在 Hausdorff 距离和 FDD 上更优，说明边界对齐和形状一致性更好。
- **域迁移鲁棒性**：在真实 CXR 上，CheXanatomy 的 Dice/IoU 与 U‑Net 持平，但几何指标（边界误差、形状描述误差）显著更低，表明对几何结构的学习更为稳定。
- **消融洞察**：
  - 增加模型规模和输入分辨率均能持续提升性能。
  - 视觉编码器微调**几乎没有影响**，解剖推理主要发生在跨模态 token 空间。
- **样本效率**：解剖预训练模型在仅有 0%–3% 训练数据时大幅优于无预训练模型，证明预训练提供了强空间先验，利于快速适配新定位任务。

### 7. 优点
- **架构简洁**：不引入任何任务专用解码器或像素级损失，完全在 token 空间完成解剖监督，保持了 VLM 的统一生成范式。
- **可扩展监督**：利用 CT 投影生成大规模、多结构、解剖一致的合成标签，无需人工 CXR 标注，方法可复用至其他解剖区域或模态。
- **几何鲁棒性提升**：在真实域迁移中表现出优异的形状保持能力，对临床部署更有意义。
- **多任务潜力**：同一模型支持语言提示、边界框定位和分割，便于嵌入接地报告生成或区域推理流水线。
- **开源代码**：提供了合成数据生成（CheXsynth）与训练框架（CheXanatomy）的完整代码。

### 8. 不足与局限
- **任务与解剖范围有限**：仅针对胸部 X 光片，且覆盖的解剖结构限制在 80 个胸部相关类别，未测试全身或跨模态扩展。
- **基线比较偏窄**：未与当前流行的视觉定位或分割大模型（如 SAM、CLIP-SAM、SegGPT 等）对比，无法评估在 SOTA 大模型分割浪潮中的相对位置。
- **合成到真实漂移**：虽在真实数据上几何鲁棒性更佳，但训练仍完全依赖合成投影，合成数据的逼真度（如散射、噪声模型）对性能有潜在影响，且文中未探讨合成质量的影响。
- **评估指标偏重几何**：缺少临床相关的下游任务验证（如病灶检测、报告生成的端到端评估）。
- **算力与训练细节不透明**：未公布训练总时长、具体 epoch 数或收敛曲线，复现和成本评估存在信息缺口。

（完）
