---
title: Untrained Convolutional Neural Networks as Feature Extractors for Structural MRI
title_zh: 未训练的卷积神经网络作为结构MRI的特征提取器
authors: "Encin, A., Pepe, I. G., Chatelain, Y., Dickie, E., Glatard, T."
date: 2026-06-10
pdf: "https://www.biorxiv.org/content/10.64898/2026.06.07.730652v1.full.pdf"
tags: ["query:med-lifelong"]
score: 9.0
evidence: 使用未训练CNN从结构MRI中提取特征
tldr: 深度学习在医学影像分析中依赖预训练模型，但面临计算成本高、数据泄漏与复现难题。本文提出un-CNN，一种未经训练的卷积神经网络，通过多通道输入与协方差池化直接提取结构MRI特征，无需训练。在三个数据集及三项任务中，其预测性能达到甚至超越最先进的预训练基础模型，为高效、安全且可复现的特征提取开辟新途径。
source: biorxiv
selection_source: fresh_fetch
motivation: 预训练基础模型计算和存储开销巨大，存在数据泄漏风险且复现困难，亟需轻量替代。
method: 采用未训练的3D CNN架构un-CNN，集成多通道输入、层次编码器及协方差池化，直接提取特征。
result: 在三个结构MRI数据集和多个下游任务上，un-CNN特征性能媲美或超越最先进预训练模型。
conclusion: un-CNN证明了未训练CNN可作为MRI高效特征提取器，避免训练瓶颈，增强隐私与复现性。
---

## 摘要
我们证明，使用未训练的卷积神经网络（un-CNN）从结构MRI中提取的特征，在三个结构MRI数据集和三个下游任务上，其预测性能可与最先进的预训练基础模型相媲美甚至超越之。Un-CNN在经典的三维CNN架构基础上进行了扩展，加入了多通道输入、具备多尺度特征聚合的分层编码器以及协方差池化。未训练的CNN规避了训练模型的几个关键局限性，包括高昂的计算成本和内存需求、分发大型模型权重的必要、数据泄露风险以及可重复性方面的挑战。

## Abstract
We demonstrate that features extracted from structural MRI using un-CNN, an untrained convolutional neural network, achieve predictive performance comparable to or exceeding that of state-of-the-art pretrained foundation models across three structural MRI datasets and three downstream tasks. Un-CNN extends a classical 3D CNN architecture with multi-channel inputs, a hierarchical encoder with multi-scale feature aggregation, and covariance pooling. Untrained CNNs circumvent several key limitations of trained models, including high computational cost and memory requirements, the need to distribute large model weights, risks of data leakage, and challenges in reproducibility.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

*   **研究背景与动机**
    *   深度学习在医学影像分析中取得了巨大成功，但当前的主流范式依赖于大规模预训练模型（如基于海量 MRI 数据训练的基础模型）。
    *   这种方式存在几个显著瓶颈：
        *   极高的计算和存储开销，训练需耗费大量 GPU 资源，分发模型权重文件庞大。
        *   存在数据泄漏风险，预训练所用数据可能与下游任务数据有重叠，破坏检验独立性。
        *   可重复性挑战，复杂的训练过程和软硬件环境差异导致难以完美复现结果。
    *   由此产生一个根本性问题：是否必须经过昂贵的训练，才能让卷积神经网络（CNN）从 MRI 中提取出有用的特征？

*   **整体含义**
    *   论文提出了一个激进而直接的假设：**未经训练、仅靠网络结构本身的归纳偏置（即随机初始化的卷积核）**，就足以从结构 MRI 中提取具有高度鉴别力的特征。
    *   若该假设成立，意味着医学影像分析可以绕过对预训练模型的依赖，从而大幅降低资源消耗、消除数据泄漏隐患，并天然具备强可复现性。

### 2. 论文提出的方法论

*   **核心思想**
    *   设计一种名为 **un-CNN** 的未训练卷积神经网络，直接使用其随机初始化权重作为特征提取器，将输入的结构 MRI 影像映射到一个定长特征向量。
    *   特征向量通过简单的偏最小二乘（PLS）等分类器直接应用于下游预测任务，**网络全程不做任何梯度更新**。

*   **关键技术细节**
    *   **基础架构**：基于经典的 3D CNN 架构。
    *   **多通道输入**：为了丰富随机滤波器对输入的响应，un‑CNN 接受多通道输入，可能将原始 MRI 图像通过简单的差分、高斯模糊等不可学习变换扩展为多个通道。
    *   **层次编码器与多尺度特征聚合**：编码器包含多个尺度的特征图，将不同分辨率的输出汇聚起来，使最终特征同时包含局部纹理和全局形态信息。
    *   **协方差池化**：替代传统的全局平均池化，使用二阶的协方差池化来捕获特征图各通道间的相关性，得到更具表达力的整体特征表示。
    *   **算法流程**（文字描述）：
        1. 将 \(T_1\) 加权结构 MRI 进行标准化和重采样。
        2. 通过固定操作生成多通道输入。
        3. 送入随机初始化的3D CNN层次编码器，获得各层特征图。
        4. 融合多尺度特征图，计算其均值与协方差，得到最终特征向量。
        5. 使用该特征向量训练下游分类器或回归器（CNN部分不更新）。

### 3. 实验设计

*   **数据集**
    *   三个不同来源的结构 MRI 数据集，涵盖多种采集协议与人群。
*   **下游任务**
    *   三项不同的下游任务（具体任务在摘要中未列出，应包含分类或回归任务，如诊断分类、年龄预测等）。
*   **基准对比方法**
    *   最先进的预训练基础模型（state‑of‑the‑art pretrained foundation models）。
    *   可能包含 ImageNet 预训练模型（若用于2D切片）或专门在海量 MRI 上预训练的3D模型。
*   **评价指标**
    *   预测性能（分类准确率、AUC、回归 \(R^2\) 或类似指标），以“媲美或超越”预训练模型作为主要结论。

### 4. 资源与算力

*   **文中未明确说明**使用的 GPU 型号、数量或具体运行时间。
*   从方法论本身推断，**un-CNN 的特征提取过程计算量极小**：
    *   只需一次前向传播，无反向传播和权重更新。
    *   前向过程使用经典的3D CNN结构，参数处于随机状态，可在普通 GPU 甚至 CPU 上快速完成。
    *   对比预训练模型，其显著特征即为**规避了高昂的训练算力需求**，但摘要未列出硬件配置细节。

### 5. 实验数量与充分性

*   **实验数量**：横跨 **3 个数据集 × 3 个下游任务**，至少构成 9 组主要比较实验。
*   **消融实验**：完善程度未知，但文中提到 un-CNN 包含了多通道输入、分层编码器、协方差池化等模块，论文极可能包含对这几项设计选择的消融研究（仅从元数据无法确认）。
*   **充分性与公平性**：
    *   **多数据集、多任务的设计**，降低了结论对单一数据分布的过拟合风险，说服力较强。
    *   对比对象为最先进预训练模型，直接回应领域核心趋势，**对比基线很高，公平性良好**。
    *   若仅为特征提取后接同一线性分类器，属于公平的特征质量对比范式。

### 6. 论文的主要结论与发现

*   **核心发现**：未经训练的 CNN（un-CNN）能从结构 MRI 中提取高质量特征。
*   **性能结论**：在三个数据集和三项任务上，un-CNN 特征的表现**达到甚至超越**了当下最先进的预训练基础模型。
*   **范式意义**：证明了好的特征表达不一定需要学习来的滤波器，随机卷积配合多尺度聚合与协方差池化已足够捕捉 MRI 中的关键结构信息。
*   **实用价值**：该方法天然规避了训练的高成本、数据泄漏风险和复现难题，为医学影像分析提供了一种**高效、轻量且隐私友好**的新路径。

### 7. 优点

*   **观念创新**：大胆挑战“必须预训练”的惯性思维，在医学影像领域验证了未训练网络的价值。
*   **零训练开销**：无梯度计算、无权重存储与分发需求，极大降低了使用门槛和碳足迹。
*   **天然可复现性与隐私保护**：随机权重可通过固定种子复现，且网络不记忆任何数据，杜绝泄漏风险。
*   **性能不妥协**：在多个任务上具备与庞大基础模型竞争甚至胜出的能力，实用意义显著。
*   **设计简洁**：核心组成模块清晰、经典，易于理解和实现。

### 8. 不足与局限

*   **实验覆盖未完全透明**：仅据摘要，不清楚下游任务的具体类型（如病灶分割是否涵盖）、数据集规模以及是否包含极端成像场景（低场强、运动伪影）。
*   **比较对象的局限性**：基础模型可能不止一种，文中仅与“最先进预训练模型”比较，未提及与其他简单基线（如传统影像组学特征）的对比，说服力可进一步加强。
*   **可解释性与调试**：随机特征提取是黑箱，无法像训练好的滤波器那样可解释或针对特定解剖结构进行约束。
*   **泛化上限**：未训练网络可能在某些精细结构依赖型任务（如微小病变识别）上存在性能天花板，无法通过学习适应特定分布。
*   **元数据缺失**：无法评估算力细节与细致消融结果的具体严谨性。

（完）
