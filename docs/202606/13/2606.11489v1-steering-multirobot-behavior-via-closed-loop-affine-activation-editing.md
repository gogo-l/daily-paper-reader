---
title: Steering Multirobot Behavior via Closed-Loop Affine Activation Editing
title_zh: 通过闭环仿射激活编辑引导多机器人行为
authors: "Satyajeet Das, Darren Chiu, Shashank Hegde, Gaurav S. Sukhatme"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.11489v1"
tags: ["query:continual"]
score: 9.0
evidence: 通过推理时编辑激活来应对多机器人策略适应中的灾难性遗忘问题
tldr: 真实多机器人系统常需执行预训练策略之外的新行为，但微调或重训练可能导致灾难性遗忘，损害基础性能。为此，提出推理时行为引导框架CLAE，通过闭环保守激活编辑来适配冻结策略。CLAE利用稀疏自编码器和强化学习，在线编辑中间层激活，实现状态依赖的仿射变换。在冻结的多四旋翼导航策略上，CLAE成功引导机器人控制速度、保持编队，并产生规避监控的全新行为，展现了无需重训即可安全扩展行为的能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11489v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1455, \"height\": 603, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11489v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1432, \"height\": 707, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11489v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1148, \"height\": 640, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11489v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1431, \"height\": 439, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.11489v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1408, \"height\": 789, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11489v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 861, \"height\": 235, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11489v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 480, \"height\": 456, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11489v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1448, \"height\": 282, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11489v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1214, \"height\": 416, \"label\": \"Table\"}]"
motivation: 现有机器人行为适配方法风险灾难性遗忘，缺乏在保持基础性能下安全引入新行为的机制。
method: CLAE训练稀疏自编码器捕获潜在特征，通过后验探测选取行为相关维度，再用强化学习学习轻量引导策略，推理时对选定特征施加闭环保守编辑。
result: 在仿真和实物四旋翼上，CLAE实现了单机器人速度控制、多机器人编队保持和全新避监控行为，均无需修改原始导航策略。
conclusion: CLAE为冻结策略提供灵活行为引导，避免重训练，保留原始能力，可扩展至多种任务，具有实用价值。
---

## 摘要
现实世界中的机器人需要使其行为适应超出预训练策略所能覆盖的范围。策略微调或重新训练是可选方案，但它们存在灾难性遗忘的风险，会降低预训练策略的基本性能。为了解决这一问题，我们提出了CLAE：闭环仿射激活编辑，这是一种推理时框架，通过编辑中间激活来引导冻结策略的行为，同时保持基础策略权重和下游动作头不变。CLAE将行为引导视为一个闭环问题，其输出对策略激活进行编辑，这些编辑根据机器人状态、环境、目标行为和多机器人上下文在线适应。它在冻结策略的激活上训练稀疏自编码器，通过事后探测选择与行为相关的潜在特征，并学习一个轻量级基于强化学习的引导策略，在推理过程中对选定的潜在特征应用状态相关的仿射编辑。我们在一个冻结的多四旋翼飞行器导航策略上验证了CLAE，该策略训练用于执行单一任务：将机器人导航至一组目标位置并避开障碍物。通过大量仿真和物理实验，我们证明在导航至目标位置的过程中，CLAE能够：1. 通过控制每个机器人的速度曲线来引导个体机器人行为；2. 通过保持期望的编队来协调多机器人行为；3. 产生全新的行为，要求机器人在环境中减少暴露于监控摄像头的可能性。

## Abstract
Real-world robots need to adapt their behavior beyond the envelope of their pre-trained policy. Policy finetuning or retraining are options, but they risk catastrophic forgetting, degrading the pretrained policy's base performance. To combat this, we introduce CLAE: Closed-Loop Affine Activation Editing, an inference-time framework for steering the behavior of a frozen policy by editing intermediate activations while keeping the base policy weights and downstream action head untouched. CLAE approaches behavior steering as a closed-loop problem whose outputs edit policy activations that adapt online to the robot state, environment, target behavior, and multi-robot context. It trains a sparse autoencoder over frozen-policy activations, selects behavior-relevant latent features via post-hoc probing, and learns a lightweight RL-based steering policy that applies state-dependent affine edits to selected latents during inference. We validate CLAE on a frozen multi-quadrotor navigation policy trained to perform a single task: navigating robots to a set of goal locations while avoiding obstacles. Through extensive simulations and physical tests, we show that while navigating to their goal positions, CLAE can 1. steer individual robot behavior by controlling each robot's velocity profile; 2. coordinate multirobot behavior by preserving a desired formation; and 3. produce entirely new behavior wherein robots are required to reduce their exposure to surveillance cameras in the environment.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：当真实世界中的机器人需要执行超出其预训练策略范围（“行为包络线”）的新行为时，如何在不改变该策略权重的前提下，安全、灵活地进行行为适配？
- **研究动机**：
    - **灾难性遗忘风险**：传统的策略微调或重新训练虽然能适应新任务，但会修改基础策略权重，可能导致原有基础行为性能下降。
    - **维护成本高昂**：为每个新行为偏好都维护一个独立的微调模型是不现实的，尤其是在向通用预训练大模型发展的趋势下。
    - **多机器人场景的复杂性**：在多机器人系统中，单个机器人的正确行为不仅取决于自身状态，还与队友状态动态相关，这进一步加剧了行为适配的挑战。
- **整体含义**：论文提出了一种“训练后行为引导”的新范式，即在推理时，通过编辑冻结策略的内部激活，而非修改权重或添加动作残差控制器，来引导机器人行为。其核心理念是，机器人行为失败可能并非缺乏底层技能，而是缺少在测试时灵活调用和组合现有技能的机制。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

CLAE框架的核心思想是：将行为引导建模为一个闭环控制问题，其输出的控制量是施加于策略中间层激活上的仿射变换参数。具体技术流程如下：

- **步骤一：构建稀疏激活基与行为相关潜在特征选择**
    - **激活接口**：确定冻结基础策略 `π_θ0` 的一个中间层 `ℓ`，将策略分为前段 `F`（输入观测，输出该层激活 `x_i,t`）和后段 `G`（输入激活，输出动作 `u_i,t`）。
    - **稀疏自编码器**：在基础策略的冻结激活 `x_i,t` 上训练一个稀疏自编码器，编码器 `E_ψ` 将密集的激活向量 `x_i,t` 映射为稀疏的潜在向量 `ℓ_i,t`，解码器 `D_ψ` 则将其重构回原始激活。
    - **行为探测与潜在特征选择**：针对每种目标行为 `b`，利用后验探测，计算每个SAE潜在维度 `j` 与行为相关指标（从基础策略rollout中计算得到）的皮尔逊相关系数。使用一个分裂稳定分数 `S(b)_j` 来衡量相关性，并选出分数最高的 `K` 个维度作为该行为相关的可编辑潜在特征集 `S_b`。

- **步骤二：学习闭环仿射激活编辑策略**
    - **仿射编辑动作空间**：学习一个轻量级的引导策略 `π_steer_{ϕ_b}`。在每个时间步，该策略输出针对选定潜在集 `S_b` 的仿射编辑参数 `ξ_i,t = (m_i,t, c_i,t)`，即乘法增益和加法偏移。
    - **闭环编辑**：将编辑作用于SAE潜在空间 `ℓ'_i,t,j = m_i,t,j ℓ_i,t,j + c_i,t,j`，仅修改被选中的潜在特征，其余保持不变。编辑后的潜在向量通过解码器重构回激活 `x'_i,t`，并传入冻结策略的后段 `G` 生成最终动作。
    - **强化学习优化**：使用PPO算法（Beta分布动作头）训练引导策略。引导策略的目标是最大化特定于行为 `b` 的累积奖励，该奖励由行为任务奖励、稳定性惩罚和碰撞惩罚组成。整个训练过程中，基础策略 `θ_0` 和SAE参数 `ψ` 均保持冻结。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

- **实验场景**：使用 QuadSwarm 仿真器，模拟8架四旋翼飞行器在布满静态圆柱形障碍物的10x10x10米房间内导航。基础策略是一个训练好的端到端、去中心化强化学习导航策略。
- **实验任务（Benchmark）**：在三个不同的行为引导任务上验证CLAE。
    1.  **速度曲线跟踪**：引导机器人跟踪由最小化 snap 规划器生成的参考速度曲线。
    2.  **多机器人编队控制**：将机器人分为两组，要求在导航至目标点的同时保持指定的方形编队。
    3.  **相机感知隐秘导航**：引入基础策略训练时未见过的监控摄像头，机器人需在导航过程中最小化被摄像头拍到的时间。
- **对比方法**：
    - **基础策略**：未经编辑的冻结策略，作为性能基线和保留基础能力的参考。
    - **固定标量 SAE 引导 [12]**：遵循先前 VLA 模型激活引导工作，沿某个行为相关的 SAE 解码器方向施加一个固定的标量偏移。
    - **线性探针干预 [15]**：拟合一个线性探针，并施加最小范数扰动，使原始激活向目标值移动。
    - **闭环仅乘法编辑**：CLAE 的消融版本，移除仿射编辑中的加法偏移项，仅保留乘法增益，以隔离完整仿射变换的效果。

### 4. 资源与算力

- 全文未明确提及用于训练引导策略的具体计算资源，例如 GPU 型号、数量或总训练时长。

### 5. 实验数量与充分性

- **实验组数**：实验设计相当充分，具体包括：
    - **3个主要行为引导任务**，每个任务均与多个基线方法进行对比。
    - **实物部署验证**：在资源受限的 Crazyflie 2.1 真实四旋翼平台上对编队控制任务进行了零样本迁移部署。
    - **多项消融研究**：围绕编队控制任务，系统性地消融了以下关键设计要素：
        1.  激活编辑 vs. 权重更新（直接微调基础策略、从头开始训练）。
        2.  引导策略的动作头设计（Beta 分布 vs. 高斯分布 vs. 高斯-tanh 分布）。
        3.  SAE 和行为探测的必要性（直接编辑原始激活）。
- **评估指标**：
    - **行为特异性指标**：速度误差（m/s）、编队误差（衡量与目标几何形状的偏差）、可观察性（被摄像头拍到的总次数）。
    - **通用安全/任务完成度指标**：无碰撞率（%）、成功率（到达目标%）、目标距离（m）。
- **客观性与公平性**：
    - 所有对比方法均作用于相同的冻结基础策略和**相同的中间激活层**，比较公平。
    - 奖励函数是特定于任务的，但形式化清晰，并在消融实验中保持了控制变量。
    - 消融实验从理论层面（灾难性遗忘、搜索空间大小、动作边界稳定性）系统验证了方法各组成部分的必要性。

### 6. 论文的主要结论与发现

- CLAE 能够在**不修改基础策略权重**的前提下，成功地引导多机器人系统的行为，使其完成预训练时未曾以主要目标学习过的新任务。
- **显著的行为引导效果**：在三个任务上，CLAE 均大幅提升了行为相关指标：
    - 速度跟踪误差降低了约 3.8 倍。
    - 编队控制误差降低了约 3.2 倍。
    - 可观察性（相机暴露）减少了约 3.1 倍。
- **有效保留基础能力**：在实现新行为引导的同时，CLAE 保持了与原始基础策略相近的高无碰撞率和任务成功率，有效避免了灾难性遗忘。
- **轻量级设计的有效性**：对冻结激活中一小部分 SAE 潜在特征的微小、有界仿射编辑，在闭环系统中累积起来，便能产生宏观层面的、显著的、甚至全新的行为效果。
- **真实世界可部署性**：整个 CLAE 流程可以被编译为 C 语言代码，在资源极度受限的微控制器上实时运行，证明了其实际应用的潜力。

### 7. 优点：方法或实验设计上有哪些亮点

1.  **新颖的行为引导思路**：从“修改权重”转向“在冻结策略内部进行状态依赖的激活编辑”，开创性地为解决机器人训练后行为适配问题提供了新路径，避免了灾难性遗忘。
2.  **高效的稀疏编辑接口**：
    - 利用**稀疏自编码器**将高维激活压缩为可解释的稀疏特征，极大缩小了编辑空间。
    - 通过**后验行为探测**自动筛选行为相关特征，实现了编辑的精确性和针对性，确保对非目标行为影响最小。
3.  **闭环控制视角**：将激活编辑视为一个与机器人和环境动态交互的闭环控制问题，使编辑参数能够根据实时状态在线调整，比施加固定偏移或开环扰动更鲁棒、更强大。
4.  **方法普适性强**：同一个激活编辑框架被成功应用于三个截然不同的任务（个体运动、多体协调、全新环境感知），展示了其作为通用框架的潜力。仅需白盒访问中间层激活，这是迈向大型预训练模型通用行为适配的重要一步。
5.  **实验验证全面扎实**：不仅在仿真环境中做了充分的量化对比和消融研究，还完成了在真实物理平台上的零样本迁移部署，从理论拆解到实际落地都进行了验证。

### 8. 不足与局限

1.  **行为相关特征选择的局限性**：对于“隐秘导航”这类基础策略训练中完全不存在的外部目标，其行为相关特征是通过“轨迹调制代理指标”（如横向运动、航向变化）间接筛选的。论文也承认，在这种场景下如何更直接、更有原则地选择相关特征是尚未解决的问题。
2.  **缺乏正式的安全性/稳定性保证**：尽管实验证明编辑后的系统运行稳定，但论文并未从理论上给出该闭环编辑系统的安全性或稳定性证明，这限制了其在安全至上场景中的应用。
3.  **验证场景相对单一**：目前仅在四旋翼飞行器的导航策略上进行了验证，虽然展示了普适性潜力，但尚未在更复杂的操作任务或大型视觉-语言-动作模型上进行测试。
4.  **依赖仿真环境**：引导策略的训练高度依赖于在仿真环境中的闭环交互。对于难以进行高精度物理仿真的复杂接触式任务，其应用会受到限制。
5.  **奖励函数设计**：和任何基于强化学习的方法一样，学习到的行为质量高度依赖于奖励函数的设计。

（完）
