---
title: Active Source-free Domain Adaptation in Open-set Medical Image Segmentation via Decomposed Uncertainty and Prototype Discrepancy
title_zh: 基于分解不确定性与原型差异的开放集医学图像分割中无源主动域适应
authors: "Jin Yang, Yichi Zhang, Peijie Qiu, Xiaobing Yu"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08749v1"
tags: ["query:med-lifelong"]
score: 9.0
evidence: 开放集医学图像分割的主动式无源域自适应，处理新类别和有限样本及隐私限制
tldr: 医学图像分割中领域偏移和开放集新类别问题并存，且源数据常因隐私不可访问。本文提出主动式源无关开放集域适应方法ASFOSDA，首次在无源数据下实现开放集分割的主动学习。方法通过类别分解不确定性和原型差异查询信息量最大的目标样本，结合目标精炼自训练生成伪标签进行半监督适应。在跨域体积分割任务中性能超越现有方法，为隐私保护下的临床部署提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08749v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1250, \"height\": 399, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08749v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1250, \"height\": 237, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08749v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1259, \"height\": 322, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08749v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1263, \"height\": 933, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08749v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1265, \"height\": 235, \"label\": \"Table\"}]"
motivation: 临床目标域包含私有解剖结构且源数据不可用，现有的闭集域适应方法无法满足开放集分割和严格隐私要求。
method: 提出ASFOSDA，利用类别分解不确定性（CDU）和类别无关原型差异（CPD）主动查询关键样本，并通过目标精炼自训练生成伪标签进行半监督学习。
result: 在跨域开放集体积医学图像分割任务上，ASFOSDA性能显著优于现有最先进的域适应方法。
conclusion: 首次实现开放集医学图像分割的无源数据主动域适应，通过不确定性与原型差异有效选择样本，增强了隐私保护下的模型泛化能力。
---

## 摘要
深度学习方法因领域偏移而难以在不同分割数据集上展现鲁棒性能，但主动域适应技术通过查询少量目标域样本进行适应训练提升了其泛化能力。然而在临床实践中，目标域常包含源数据未呈现的私有类（新解剖结构或病理），而现有方法采用源域和目标域分割类别相同的封闭集分割。此外，由于严格的数据隐私规定，适应过程中源数据通常无法访问。为解决这些局限，我们提出了一种主动式无源开放集域适应（ASFOSDA）方法，这是首个在无法获取源数据的情况下，为开放集医学图像分割中的深度学习模型实现主动学习的工作。该方法采用基于类别感知分解不确定性（CDU）和类别无关原型差异（CPD）的主动开放集查询策略，选择最具信息量的目标样本进行训练。CDU通过在随机过程中运用测试时增强，衡量样本的偶然不确定性和模型认知不确定性。CPD衡量跨域和自域差异以选择多样化样本。随后，为通过增强训练样本提升适应性能，提出了一种目标优化自训练策略，为未选择样本生成高质量伪标签，从而将其与标注样本结合进行半监督训练。我们在跨域开放集体积医学图像分割任务上评估了该方法，其性能超越了当前最先进的适应方法。

## Abstract
Deep learning (DL) methods are challenged to demonstrate robust performance across different segmentation datasets due to domain shifts, but active domain adaptation techniques enhance their generalization performance by querying a few samples from target domains for adaptation training. However in clinical practice, target domains often include private classes of new anatomical structures or pathologies that are not presented in the source data, and existing methods implement closed-set segmentation where source and target domains have the same segmentation classes. Additionally, source data are often inaccessible during adaptation due to strict data privacy regulations. To address these limitations, we propose an Active Source-free Open-set Domain Adaptation (ASFOSDA) method which is the first work to implement active learning for adapting DL models in open-set medical image segmentation without the access to source data. This method employs an active open-set query strategy to select the most informative target samples for training models based on Class-aware Decomposed Uncertainty (CDU) and Class-agnostic Prototype Discrepancy (CPD). CDU measures sample aleatoric uncertainty and model epistemic uncertainty by employing test time augmentation in stochastic processes. CPD measures cross-domain and self-domain discrepancy for selecting diverse samples. Subsequently, to boost the adaptation performance by enhancing training samples, a Target-refined Self-training strategy is proposed to generate high-quality pseudo labels for unselected samples, thus combining them with labeled samples for a semi-supervised training. We evaluated our method on cross-domain open-set volumetric medical image segmentation tasks, and it outperformed state-of-the-art adaptation methods.

---

## 论文详细总结（自动生成）

## 一、核心问题与研究动机  
- **领域偏移与开放集挑战**：深度分割模型因数据分布差异（domain shift）难以泛化，而临床目标域常包含源数据未标注的新解剖结构或病理（私有类）。以往域适应方法多假设闭集（源域与目标域类别一致），在遇到新类别时会导致负迁移。  
- **源数据不可访问**：严格的数据隐私法规往往使源域图像在适应阶段无法使用，需要源无关（source-free）的域适应。  
- **主动学习缺口**：现有主动源无关域适应（ASFDA）方法缺乏对未知类信息量的评估，尚未有工作在开放集医学图像分割中同时实现无源与主动学习。  
- **本文目标**：首次提出主动式无源开放集域适应方法（ASFOSDA），在无源数据条件下，通过主动查询少量目标样本实现开放集分割模型的适应。

## 二、方法论  
### 整体框架  
ASFOSDA 分为两个主要阶段：**主动开放集查询策略**（Active Open-set Query Strategy）和**目标精炼自训练**（Target-refined Self-training）。源模型仅在初始化时使用源数据，之后所有适应均在无源条件下进行。

#### 1. 主动开放集查询策略  
该策略由 **类别感知分解不确定性（CDU）** 和 **类别无关原型差异（CPD）** 组成，用于从无标注目标域中选出最有信息量的样本进行标注。

- **类别感知分解不确定性（CDU）**  
  - 对每个无标注样本 \( x_u \) 使用测试时增强（TTA）生成多个变体 \( x_u^1 \dots x_u^m \)。  
  - **类别感知偶然不确定性（CAU）**：仅针对未知类别（目标域私有关别 \( C_u \)），计算预测概率图的熵 \( H(x_u) \) 以及增强变体的平均熵，再取两者平均得 CAU（公式1-3）。  
  - **类别感知认知不确定性（CEU）**：基于 logits 计算未知类别的预测能量 \( E(x_u) \)（公式4、5），并计算原始样本与增强变体之间能量的方差（公式6）。  
  - CDU 为 CAU 与 CEU 经最小-最大归一化和均匀分位数变换后之和（公式7）。高 CDU 值代表样本对未知类的预测更不确定。

- **类别无关原型差异（CPD）**  
  - 使用源训练模型的编码器提取特征。选择 CDU 低的样本作为**原型**（代表源域知识），CDU 高的样本作为**候选查询样本**。  
  - **跨域差异（CDD）**：计算每个高不确定性候选与所有低不确定性原型之间的余弦距离均值（公式8），度量源‑目标分布差距。  
  - **自域差异（SDD）**：计算候选与其他高不确定性候选之间的余弦距离均值（公式9），避免选择冗余样本。  
  - CPD = CDD + SDD（公式10），得分高的样本信息量大且具代表性，被优先选中进行标注。

#### 2. 目标精炼自训练  
- 先用已标注的查询样本集 \( \mathcal{T}_l \) 进行全监督微调。  
- 再使用训练后的模型为剩余未选样本 \( \mathcal{T}_u \) 重新生成并**精炼**伪标签。  
- 最后将标注样本与伪标注样本结合进行**半监督训练**，联合监督损失 \( L_{sup} \) 和无监督损失 \( L_{unsup} \) 进一步提升适应性能。

## 三、实验设计  
### 数据集与跨域开放集任务  
- **CT 场景**  
  - **MSD Spleen → FLARE**：从单一脾脏分割（1类）转到腹腔4器官（肝、肾、胰、脾）分割。  
  - **FLARE → AMOS CT**：从4类转到14器官分割。  
- **MR 场景**  
  - **Abdominal MR → AMOS MR**：从8类（脾、肾、肝等）转到13器官分割。  
所有目标域均包含源域未见的新类别，构成开放集设定。

### 对比方法  
- **下界**：Source Only（源模型直接测试，无适应）。  
- **上界**：Target Supervision（使用全部目标域标注微调）。  
- **无源域适应（SFDA）**：DPL, FSM, TT-SFUDA。  
- **主动源无关域适应（ASFDA，均按 5% 查询预算）**：RAND（随机选择）、PENT、LC、PMAR、Core-set、MAXREP、CENT、UGTST。  
- 本方法 ASFOSDA（同样查询 5% 样本，并利用未选样本自训练）。

### 实现细节  
- 网络：3D U-Net 和 Swin UNETR。  
- 损失函数：监督训练用 Dice Loss + 交叉熵，自训练同样组合。  
- 优化：AdamW，初始学习率 0.001，多项式衰减（power 0.9）。  
- 批次大小：2，patch 尺寸 96³。  
- 评估指标：Dice 相似系数（DSC）和 95% Hausdorff 距离（95HD），结果用均数±标准差报告，并采用 Mann-Whitney U 检验统计显著性。

## 四、资源与算力  
- 硬件：**单张 NVIDIA Tesla A100 PCI-E Passive GPU**。  
- 源模型初始化训练 1000 epochs，目标域适应训练 500 epochs，使用 PyTorch 实现。  
- 论文未给出具体训练时长，仅描述 batch size 及 patch 大小，但可推断为常规的 3D 分割实验规模。

## 五、实验数量与充分性  
- **主实验**：3 个跨域开放集任务 × 2 种网络架构，总计 6 组比较，每组均对比 11 种方法（包含上下界、3 种 SFDA、8 种 ASFDA）。  
- **消融实验**：  
  - 查询预算从 0% 到 20% 的敏感性分析。  
  - 原型/候选数量 \(K\) 的取值影响（5% 预算下）。  
  - 不同不确定性指标（PENT、LC、PMAR、CAU、CEU、CDU）的对比。  
  - 查询策略中 CAU、CEU、CPD 及自训练模块的逐步添加，验证各组件贡献。  
- 所有消融均在同一任务上进行，结果以表格和图表呈现，并附统计检验。整体实验设计系统、客观，能充分证明方法的有效性和各组件的作用。

## 六、主要结论与发现  
- ASFOSDA 在所有跨域开放集任务和两种网络下，均取得**最优 DSC 和最低 95HD**，显著超越所有 SFDA 和 ASFDA 方法（p<0.01）。  
- 在 MSD→FLARE 任务上，DSC 达到上界的 84% 以上；在 FLARE→AMOS CT 上约 85%；在 MR 适应中亦保持较高相对上界。  
- CDU 能有效分离偶然与认知不确定性，且仅针对未知类别度量，比传统熵、置信度等更精准；CPD 通过跨域和自域差异度量避免冗余，选出代表性强且分布差异大的样本。  
- 目标精炼自训练能通过伪标签扩展训练集，进一步增强适应性能。

## 七、优点  
- **问题新颖性**：首次将主动式无源域适应拓展到开放集医学图像分割，同时应对新类别、隐私限制和标注成本三大挑战。  
- **不确定性度量精细**：CDU 分解偶然/认知不确定性，且类感知（仅关注未知类），利用 TTA 在随机过程中估计，更具可解释性。  
- **样本选择兼顾信息量与多样性**：CPD 结合跨域和自域差异，避免选择冗余样本，提升标注效率。  
- **有效利用未标注数据**：通过精炼伪标签进行半监督训练，在极低标注预算下进一步挖掘目标域信息。  
- **实验对比全面**：涵盖多个数据集、多种网络和大量基准方法，并配有消融验证各组件，严谨性较高。

## 八、不足与局限  
- **开放集场景有限**：仅涉及腹部器官分割，未知类数量增量较为固定，其他解剖部位或更复杂的类别差异未验证。  
- **计算开销未分析**：TTA、特征提取及原型计算可能带来额外时耗，文内未讨论实时性或效率问题。  
- **伪标签质量依赖**：目标精炼自训练虽然改善了伪标签，但在极端分布偏移下仍可能引入噪声，且半监督训练的稳定性未深入分析。  
- **隐私前提假设**：尽管源数据不可访问，但初始源模型仍需源域训练，现实中源域和模型获取仍可能存在限制。  
- **比较方法公平性**：基准 ASFDA 方法原本为闭集设计，直接用于开放集可能不利，但文中未提及是否对其进行适配；若未适配，对比可能偏向本方法。  

（完）
