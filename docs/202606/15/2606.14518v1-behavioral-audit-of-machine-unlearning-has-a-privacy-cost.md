---
title: Behavioral Audit of Machine Unlearning Has a Privacy Cost
title_zh: 机器反学习的行为审计具有隐私代价
authors: "Liou Tang, James Joshi, Ashish Kundu"
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.14518v1"
tags: ["query:llm-anonym"]
score: 9.0
evidence: 机器遗忘审计与隐私成本
tldr: 机器遗忘（MU）审计在模型所有者与审计者互不信任的场景下缺乏统一方案，现有行为审计可能泄露训练数据成员隐私。本文通过信息论证明，对凸模型仅依赖查询行为信号的通用审计无法避免隐私与审计的固有权衡——识别未充分遗忘模型必然暴露保留集成员信息。实证表明该张力在非凸模型中也持续存在，呼吁设计隐私保护的MU审计流程。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.14518v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 886, \"height\": 1071, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14518v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1375, \"height\": 553, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14518v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1376, \"height\": 521, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14518v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1800, \"height\": 1152, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14518v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 883, \"height\": 841, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14518v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 887, \"height\": 887, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.14518v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 847, \"height\": 462, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14518v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 824, \"height\": 306, \"label\": \"Table\"}]"
motivation: 现有MU审计工作未考虑模型所有者不诚实而审计者诚实但好奇的互不信任设定，行为审计可能泄露训练数据隐私，缺乏理论基础。
method: 利用信息论证明，分析仅依赖查询模型行为信号的通用审计方案，推导凸模型下隐私-审计的下界权衡。
result: 理论证明对凸模型，行为审计识别未充分遗忘时必然泄露保留集成员信息；实验证实该隐私-审计张力在凸与非凸模型中均存在。
conclusion: MU审计面临固有的隐私-审计权衡，需更谨慎设计隐私保护审计方案，为后续审计机制安全性研究提供基础。
---

## 摘要
通过机器反学习（MU）从机器学习模型中移除已学习数据的方法已被广泛研究；然而，至今尚未有公认的MU审计方案。现有工作表明，不诚实的模型所有者可以伪造证据以避免执行MU，而好奇的审计者（以及对手）即使仅有限访问，也能推断出模型及其训练数据的隐私敏感属性。然而，在模型所有者与审计者之间相互不信任的情况下对MU进行审计，仍未被探索。我们为这种场景提供了一个信息论证明：对于凸机器学习模型，一种仅依赖于查询模型以获取行为信号的通用审计方案，无法在不泄露保留集合的成员信息的情况下，识别出未充分遗忘的模型。因此，在不诚实模型所有者和诚实但好奇的审计者的假设下，对MU的审计面临固有的隐私-审计权衡。我们在凸模型上的实证结果有力地支持了这一结论，而进一步的实验表明，这种隐私-审计张力在非凸模型中仍然存在。我们的结果呼吁在现实的审计者威胁模型下更仔细地考虑隐私-审计张力，并为更严格审查MU流水线的隐私保护审计方案设计奠定基础。我们还在 https://github.com/LiouTang/Behavioral-Unlearn-Audit 发布了代码实现。

## Abstract
The removal of learned data from Machine Learning models through Machine Unlearning (MU) has been widely studied; however, there has yet to be an agreed-upon scheme for auditing MU. Existing work has shown that a dishonest model owner can falsify evidence to avoid executing MU, while curious auditors (and adversaries) can infer the privacy-sensitive properties of the model and its training data even with limited access. Yet auditing of MU under mutual distrust between the model owner and the auditor remains unexplored. We provide an information-theoretic proof for this scenario: for convex ML models, a generic audit scheme that relies solely on querying the model for \textit{behavioral} signals cannot identify insufficiently unlearned models without revealing membership information of the retained set. Therefore, auditing MU under the assumption of a dishonest model owner and an honest-but-curious auditor faces an inherent privacy-audit tradeoff. Our empirical results on convex models strongly supports this result, while further experiments demonstrate that this privacy-audit tension persists in non-convex models. Our results call for a more careful consideration of the privacy-audit tension under a realistic auditor threat model, and serve as a foundation for more scrutiny of designs of privacy-preserving audit schemes for the MU pipeline. We also release our code implementation at https://github.com/LiouTang/Behavioral-Unlearn-Audit.

---

## 论文详细总结（自动生成）

好的，这是根据您提供的论文内容生成的结构化中文总结。

### 1. 论文的核心问题与整体含义

*   **研究动机**：机器反学习允许用户请求从已训练的机器学习模型中移除其数据的影响。然而，如何验证模型所有者是否真的执行了反学习（即审计）是一个尚未解决的难题。
*   **核心问题**：现有研究通常只考虑单方面的不诚实或好奇，但在现实场景中，模型所有者可能不诚实（伪造反学习），而审计者（即使是诚实的）也可能好奇（试图推断未在反学习范围内的保留数据隐私）。本文首次探索了这种**相互不信任**场景下，审计与隐私之间的根本性矛盾。
*   **整体含义**：论文旨在证明，在模型所有者和审计者互不信任的设定下，存在一种**固有的“隐私-审计”权衡**。即，任何仅通过查询模型行为来执行审计的方案，都无法在有效检测出“未充分遗忘”的同时，防止审计者推断出保留集数据的隐私信息（如成员关系）。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

论文的核心方法论是**信息论证明**，在凸模型假设下，从理论上推导出审计有效性（合规性误差）与隐私泄露（好奇心优势）之间的下界关系。整个证明过程如下：

*   **威胁与博弈模型**：
    *   **角色**：不诚实的模型所有者（`Chal`）与诚实但好奇的审计者（`Aud`）。
    *   **模型**：`Chal`发布一个`η`-诚实反学习模型（`η=1`为完全诚实，`η<1`为部分诚实）。`Aud`可以对该模型进行有限次查询（`T`），并接收带高斯噪声的后验概率响应。
    *   **博弈**：定义了两个核心博弈游戏，即**合规性游戏**（检测`η`是否为1）和**好奇心游戏**（推断某个样本`z*`是否在保留集`Dr`中）。
*   **核心思想：信号耦合**
    *   **合规性信号**：`η`-诚实模型与`1`-诚实模型在查询点`x`上的输出差异。
    *   **好奇心信号**：保留样本`z*`的模型与未保留样本`z*`的模型在查询点`x`上的输出差异。
    *   **关键发现**：在凸模型和牛顿更新步骤下，这两种信号可以通过样本的“白化梯度”进行分解，它们之间由一个仅依赖于被遗忘集`Du`和目标样本`z*`的**对齐系数`ρ(z*)`** 所耦合。
*   **关键技术细节与推导流程**：
    1.  **桥接不等式（Lemma 3.2）**：这是证明的核心。它建立了每个查询点上，好奇心信号的绝对值下界，该下界主要由一个缩放后的合规性信号绝对值、一个由对齐系数决定的残差项以及泰勒展开误差项组成。缩放因子 **`κ`** 取决于对齐系数`ρ`、诚实度`η`以及目标样本和被遗忘集的梯度范数。公式表达了：好奇心信号 ≳ κ × 合规性信号 - Cauch-Schwarz残差 - 误差项。
    2.  **隐私-审计权衡定理（Theorem 3.3）**：基于莱卡姆两点法，将单查询的关系聚合到整个审计过程中。该定理证明了**审计的错误率（`α+α‘`）与审计者的好奇心优势（`β`）之间存在一个不可逾越的下界**。在渐近条件下（样本量`n`远大于查询预算`T`），这个下界简化为：审计者的隐私推断优势（`β`）至少为 `min(κ， 1) × (1 - α - α')`。
    3.  **推论**：该定理直接得出**互斥结论**：要么审计无法成功（`β`→0 迫使 `α+α‘`→1），要么成功的审计必然伴随非平凡的隐私泄露（`α+α’`→0 迫使 `β`→ `min(κ， 1)`）。这即为隐私-审计的固有权衡。

### 3. 实验设计：使用了哪些数据集/场景，它的benchmark是什么，对比了哪些方法

*   **数据集与场景**：
    *   **凸模型验证**：使用一个手动生成的二维四分类数据集，训练**多项逻辑回归模型**（L2正则化确保强凸性）。
    *   **非凸模型扩展**：在两个更高维度的基准数据集上进行验证：
        *   **Purchase-100**：一个包含197，324个样本、100个类别的购物数据集，使用**全连接神经网络**（4层，1024维）。
        *   **CIFAR-10**：一个包含50，000张32x32彩色图像的10分类数据集，使用**ResNet-18**模型。
*   **Benchmark与方法对比**：
    *   **反学习算法**：
        1.  对于凸模型，采用基于**(ε， δ)-认证的反学习算法**作为基准诚实反学习（`η=1`）。
        2.  对于非凸模型，对比了两种主流的近似反学习算法：**梯度上升**和**Saliency Unlearning (SalUn)**。
    *   **审计方法对比**：
        *   **凸模型**：通过理论公式直接计算以选择最优查询点。
        *   **非凸模型**：采用并适配**似然比攻击（LiRA）** 方法。具体地，通过训练并反学习多个代理模型（Purchase-100：128个，CIFAR-10：32个），拟合关于合规性和好奇心跳世界的后验概率的高斯分布，以此进行假设检验。
    *   **评估指标**：
        *   **合规性误差 (`α+α‘`)**：区分`η`-诚实模型与`1`-诚实模型的错误率。
        *   **好奇心优势 (`β`)**：推断目标样本`z*`是否在保留集`Dr`中的分辨能力（总变异距离）。

### 4. 资源与算力

*   论文**未明确提及**具体的算力配置，例如所用GPU的型号、数量或整体训练时长。
*   文中仅提及代理模型的数量（Purchase-100训练了128个，CIFAR-10训练了32个），但没有给出任何硬件资源或时间成本的细节。

### 5. 实验数量与充分性

*   **实验数量**：实验设计较为全面，覆盖了多种场景以验证理论。
    *   **凸模型实验**：包含了信号分布可视化、缩放系数`κ`在不同反学习诚实度(`η`)、不同目标样本(`z*`)和被遗忘集大小`|Du|`下的变化分析，以及最终的**隐私-审计权衡帕累托曲线**。每组实验均报告了30个随机种子的误差条。
    *   **非凸模型扩展实验**：在两个不同数据集（Purchase-100， CIFAR-10）、两种反学习算法（GA， SalUn）、不同反学习诚实度（`η=0.5， 0.9`）以及不同遗忘方式（随机采样、类别式遗忘）下，都对(`α+α‘`， `β`)的散点图和质心轨迹进行了分析。对于每个查询预算`T`，重复了30次查询点选择实验。
*   **实验充分性与客观性**：
    *   **充分性**：实验从理论验证（凸模型）到现象拓展（非凸模型）逻辑清晰，覆盖了关键参数（`η`， `|Du|`， `T`， `σ`）的影响。对不同算法和遗忘方式的对比也增加了结论的普适性。
    *   **客观性与公平性**：实验报告了多次运行的误差，并使用了公开数据集和标准模型架构。对比的`GA`和`SalUn`算法采用了原论文推荐的超参数，审计方法也基于标准的`LiRA`攻击，确保了对比的公平性。帕累托曲线的绘制清晰地展示了理论与实践的差距以及权衡关系的存在。

### 6. 论文的主要结论与发现

*   **存在根本性权衡**：在模型所有者不诚实、审计者诚实但好奇的设定下，机器反学习（MU）存在一个**固有的“隐私-审计”权衡**。一个仅依赖行为查询的通用审计方案无法同时做到既有效审计又保护隐私。
*   **理论证明与实证支持**：
    1.  对于**凸模型**，信息论下界和实证结果都强有力地证明，降低审计的合规性误差（`α+α‘`）必然会导致审计者对保留数据成员关系的推断能力（`β`）上升。两者之间存在由系数`κ`决定的帕累托前沿。
    2.  这种隐私与审计的张力在**非凸深度神经网络模型**中同样持续存在，尽管无法推导出精确的闭孔下界，但实证趋势保持一致。
*   **关键参数影响**：
    *   反学习越“诚实”（`η`越大），模型更新越彻底，使得检测`η<1`的审计变得更难（`α+α’`增大），但并未提供更强的隐私保护（`β`保持不变）。
    *   被遗忘集(`Du`)越大，单个样本的隐私泄露风险（`κ`）越低，但最坏情况（如目标样本与`Du`高度对齐）的隐私泄露依然显著。

### 7. 优点：方法或实验设计上有哪些亮点

*   **问题视角新颖**：首次将机器反学习审计的考察视角从单方面欺骗或窥探，提升到**模型所有者与审计者互不信任**的更现实场景，并点明了两者目标之间的根本矛盾。
*   **理论支撑坚实**：通过信息论方法，为直觉上的隐私-审计矛盾提供了**严谨的数学证明**，给出了可信的量化下界，为该领域的研究提供了理论基础和指导方向。
*   **理论联系实际**：不仅限于理论推导，还通过精心设计的凸模型实验验证理论，并**将现象扩展到更具现实意义的非凸深度模型中**，增强了结论的说服力和普适性。
*   **分析的启发性**：对缩放系数`κ`的详细分析（图2）指出了影响权衡的关键因素（如对齐度、被遗忘集大小），为缓解或利用此权衡关系提供了有价值的洞见。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

*   **理论的强假设局限**：
    *   **凸模型假设**：核心定理`Theorem 3.3`严格依赖于凸模型的几何性质，其结论无法直接、严密地推广到非凸深度学习模型。尽管实证观察了类似现象，但缺乏理论支撑。
    *   **查询独立性假设**：理论假设审计者选择的查询`xt`不依赖于目标样本`z*`。论文也承认，任何试图设计特定查询来规避此权衡的行为，本身就**泄露了保留集的成员信息**，因此该假设在实际中是一种必要约束。
*   **实验覆盖的局限**：
    *   **模型种类有限**：非凸模型实验仅覆盖了全连接网络和ResNet-18，探索的模型架构种类有限。
    *   **攻击形式单一**：好奇心游戏仅针对“成员关系推断”这一种隐私属性。虽然论文在第5.3节讨论了扩展到属性推断等场景的可能性，但并未进行实验验证。
*   **下界紧密度**：论文提到（如在图3中），实证观察到的`β`值**显著高于理论下界**。这说明推导出的下界并非一个紧的下界，理论的量化预测能力有限。
*   **审计策略简单**：非凸模型实验采用的是直接适配的标准LiRA方法，未探索更复杂或自适应的审计策略，可能未完全揭示该场景下最极致的隐私-审计权衡关系。

（完）
